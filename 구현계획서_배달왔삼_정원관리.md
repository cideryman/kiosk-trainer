# [배달왔삼] 이용신청 개편 및 실시간 정원 기반 즉시 주문 전환 계획서

한 달간의 시범 운영 결과(신청 1건)를 반영하여, 이용 장벽이 높았던 **'사전 이용신청 → 관리자 승인 → 날짜 맞춰 재주문'** 구조를 폐지합니다. 
대신 **'실시간 운영 상황판(소개 페이지) + 운영 시간 내 즉시 주문(누구나 가능)'** 구조로 전환하고, 훈련 현장의 과부하를 막기 위한 **삼중 안전장치(총 주문 정원, 배달 주문 정원, 온기 기반 수량 제한)**를 관리자가 유연하게 설정할 수 있도록 시스템을 개편합니다.

> [!IMPORTANT]
> **본 문서는 구현 계획서 단계이며, 실제 소스 코드 수정은 일체 진행하지 않았습니다.**
> 사용자의 검토 및 피드백/승인 이후 실제 구현 단계로 착수합니다.

---

## 1. 주요 변경 방향 및 정책

```
[ 기존 방식: 2단계 신청 허들 ]
방문자 ──> [사전 이용신청서 작성] ──> [관리자 승인 대기] ──> [문자 안내] ──> [지정일에 주문] (이탈 심각)

[ 개편 방식: 투명한 실시간 주문 ]
방문자 ──> [소개 및 실시간 상황판] ──> [운영시간 내 즉시 주문하기] (진입장벽 해소)
                 │
                 ├── ● 오늘 주문 현황: [접수 중] (남은 정원: 3건 / 배달 잔여: 1건)
                 ├── ● 배달 가능 지역: 관리자 설정 (예: 영주시 동 지역)
                 └── ● 안전 통제:
                       - 총 정원(예: 5건) 마감 시 전체 주문 마감
                       - 배달 정원(예: 2건) 마감 시 "매장 픽업"만 선택 가능
                       - 1인당 수량: 기존 온기(Credit) 시스템으로 자연스럽게 1~2잔 통제
```

---

## 2. 사용자 검토 및 결정 필요 사항 (User Review Required)

> [!NOTE]
> 아래 항목들은 관리자 화면에서 언제든 직접 수정할 수 있도록 기본값을 설정할 예정입니다.

1. **배달 가능 지역 기본 안내 문구**:
   - 제안 기본값: `"영주시 동 지역 (가흥동, 영주동, 휴천동 등)"`
   - 읍·면(풍기, 봉현 등) 제외 안내가 소개 페이지와 주문서 배달 선택 시 함께 표시됩니다.
2. **초기 정원 기본값**:
   - 총 주문 건수: **5건** (관리자가 1~30건 사이로 조절 가능)
   - 배달 최대 건수: **2건** (관리자가 0~10건 사이로 조절 가능, 0건 설정 시 포장 전용 모드)
3. **1인당 수량 조절(온기 정책)**:
   - 신규/게스트 지급 기본 온기(현재 10개)를 메뉴 1~2잔 가격 수준(예: 6~8개)으로 맞추거나 메뉴별 온기 가격을 조정하여 1인당 최대 2잔 수준으로 자연스럽게 유도합니다.
4. **동일 기기(브라우저) 반복 주문 방지**:
   - 1차 주문 완료 후 곧바로 2차, 3차 주문을 넣어 혼자 정원을 독점하지 못하도록, 동일 운영 회차 내에서는 브라우저당 1회 주문으로 제한합니다.

---

## 3. 세부 구현 계획 (Proposed Changes)

### Component 1: 관리자/주방 운영 설정 (`kitchen.html`, `js/kitchen.js`, `gas/60_Settings.gs`)
운영자가 현장 상황에 맞춰 정원과 배달 지역을 손쉽게 수정할 수 있도록 설정 UI를 추가합니다.

#### [MODIFY] [kitchen.html](file:///c:/Users/user/Desktop/키오스크/kitchen.html)
- 주간 자동 운영 및 추가 운영 섹션 하단에 **"🎯 실시간 주문 정원 및 배달 설정"** 추가
  - `오늘의 총 주문 정원 (건)` (숫자 입력, 기본값 5)
  - `오늘의 최대 배달 건수 (건)` (숫자 입력, 기본값 2)
  - `배달 가능 지역 안내 문구` (텍스트 입력, 기본값 "영주시 동 지역 (가흥동, 영주동, 휴천동 등)")

#### [MODIFY] [js/kitchen.js](file:///c:/Users/user/Desktop/키오스크/js/kitchen.js)
- 운영 설정 로드 시 새 설정값 바인딩 및 저장 이벤트 처리
- 저장 버튼 클릭 시 `updateGuestSettings` API를 통해 백엔드에 반영

#### [MODIFY] [gas/60_Settings.gs](file:///c:/Users/user/Desktop/키오스크/gas/60_Settings.gs)
- `getDefaultGuestSettings()`에 새 설정 키 추가:
  - `guestMaxOrderCount`: `5`
  - `guestMaxDeliveryCount`: `2`
  - `guestDeliveryArea`: `'영주시 동 지역 (가흥동, 영주동, 휴천동 등)'`
- `getGuestSettings` 및 `updateGuestSettings`에서 해당 키들을 안전하게 읽고 저장하도록 허용 목록 확장

---

### Component 2: 소개 및 실시간 상황판 개편 (`guest-apply.html`, `css/guest-apply.css`, `js/guest-apply.js`)
기존 신청서 폼을 걷어내고, 방문객이 즉시 현황을 알고 참여할 수 있는 **'라이브 대시보드형 소개 페이지'**로 전환합니다.

#### [MODIFY] [guest-apply.html](file:///c:/Users/user/Desktop/키오스크/guest-apply.html)
- **상단 라이브 운영 상황판(Hero Dashboard)**:
  - 현재 상태 뱃지: `🟢 지금 주문 가능` / `🟡 준비 중` / `🔴 오늘 주문 마감`
  - 운영 일정 안내: `"정기 운영: 매주 수요일 12:00~14:00"` (추가 운영일 있을 경우 함께 표시)
  - 실시간 잔여 슬롯: `"남은 주문: N건 / 5건"` · `"배달 잔여: M건 / 2건"` (배달 마감 시 `"배달 마감 (포장만 가능)"` 표시)
  - 배달 가능 지역 뱃지: 관리자가 설정한 `guestDeliveryArea` 표시
  - 대형 액션 버튼: **[지금 주문하러 가기]** (운영 중 & 잔여 슬롯 있을 때만 활성화)
- **복잡한 신청서 폼(`form`) 영역 제거**:
  - 신청 폼 대신 **"달곰이 배달 수칙 안내"** 카드 배치:
    1. 배달 주문 시 정확한 장소와 연락처가 필요합니다.
    2. 부재 중이거나 연락이 닿지 않을 경우 배달이 어려울 수 있습니다.
    3. 발달장애인 훈련생들이 정성껏 준비하므로 조금 늦더라도 너그럽게 기다려 주세요.
- 브랜드 스토리 및 생생한 고객 후기([reviews-public.html](file:///c:/Users/user/Desktop/키오스크/reviews-public.html)) 연계 유지

#### [MODIFY] [js/guest-apply.js](file:///c:/Users/user/Desktop/키오스크/js/guest-apply.js) (또는 인라인 스크립트)
- 정기/추가 운영 시간 및 실시간 주문 잔여량 조회 API 연동
- 잔여 슬롯 상태에 따라 버튼 활성화/비활성화 및 카운트다운/안내 문구 렌더링

---

### Component 3: 주문 화면의 실시간 안전 통제 (`guest.html`, `js/guest.js`, `gas/40_Orders.gs`)
사전 신청 없이 누구나 들어오더라도 현장이 안전하게 유지되도록 통제 로직을 강화합니다.

#### [MODIFY] [guest.html](file:///c:/Users/user/Desktop/키오스크/guest.html) & [js/guest.js](file:///c:/Users/user/Desktop/키오스크/js/guest.js)
- **배달 옵션 동적 제어**:
  - 당일 배달 주문 수가 `guestMaxDeliveryCount`에 도달하면 `[배달]` 옵션을 비활성화(disabled)하고, *"금일 배달 정원이 마감되어 매장 픽업만 가능합니다"* 문구 노출
  - 배달 선택 시에만 필수 입력 필드 활성화:
    - 수령인 성함/호칭
    - 연락처 (휴대폰 번호)
    - 상세 배달 장소 (건물명, 층·호수)
    - 배달 메모
    - 약식 개인정보 수집/파기 안내 ("배달 확인 및 연락 목적으로 수집하며 배달 완료 후 파기됩니다")
  - 포장/픽업 선택 시에는 닉네임만 입력받아 빠르고 가볍게 주문 완료
- **전체 정원 마감 제어**:
  - 당일 총 주문 수가 `guestMaxOrderCount`에 도달하면 장바구니/주문하기 버튼을 차단하고 "금일 준비된 수량이 모두 소진되었습니다" 안내
- **중복 주문 방지**:
  - 1회 주문 완료 시 로컬 스토리지에 해당 운영 회차 식별자 기록 ➔ 동일 회차 재주문 시도 차단

#### [MODIFY] [gas/40_Orders.gs](file:///c:/Users/user/Desktop/키오스크/gas/40_Orders.gs)
- 서버 측 주문 트랜잭션(`placeOrder`) 안전장치:
  - 동시성(Race Condition) 방어: 주문 저장 직전 당일 완료/접수 건수 재집계
  - 총 주문 건수 초과 시 또는 배달 정원 초과 시 서버 차원에서 정중한 에러 메시지 반환 (`"주문 정원이 방금 마감되었습니다."`)

---

## 4. 검증 계획 (Verification Plan)

### 수동 및 시나리오 검증
1. **관리자 설정 검증**:
   - `kitchen.html`에서 총 주문 3건, 배달 1건, 배달 지역 텍스트 변경 후 저장 ➔ 페이지 새로고침 시 정상 유지 확인
2. **소개 페이지 실시간 연동 검증**:
   - 비운영 시간에 `guest-apply.html` 접속 시 "다음 운영 시간: 수요일 12:00" 및 버튼 비활성화 확인
   - 운영 시간 전환 시 "주문 가능", 남은 슬롯 수치 정상 표시 확인
3. **배달 정원 마감 검증**:
   - 배달 주문 1건 접수 ➔ 다음 사용자의 `guest.html`에서 [배달] 버튼 비활성화 및 [매장 픽업]만 선택 가능한지 확인
4. **전체 주문 정원 마감 검증**:
   - 정원(예: 3건) 도달 시 소개 페이지 및 주문 페이지에서 즉시 "오늘 주문 마감"으로 전환되는지 확인
5. **배달 정보 필수 입력 및 픽업 간소화 검증**:
   - 배달 선택 시 연락처/장소 미입력 시 제출 불가 확인
   - 픽업 선택 시 연락처 없이 가볍게 주문 완료 확인
6. **동일 브라우저 반복 주문 차단 확인**:
   - 주문 완료 직후 뒤로가기 또는 재접속하여 추가 주문 시도시 안내 문구와 함께 차단되는지 확인

---

## 5. 다음 단계
위 구현 계획서의 내용(설정 항목, 화면 구성, 마감 로직)을 검토해 주시고, 승인해 주시면 단계별로 안전하게 구현을 시작하도록 하겠습니다.
