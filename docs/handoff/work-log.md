# 완료 작업 로그

[핸드오프로 돌아가기](../../handoff.md)

이 문서에는 최종 상태가 `완료`인 작업만 기록합니다. 구현·배포·수동검증이 남은 작업과 보류 과제는 기록하지 않습니다. 이전 혼합 기록은 [2026-07-13 작업 로그 아카이브](work-log-archive-2026-07-13.md)에 보관합니다.

## 2026-07-21 - 내 주문 확인 화면 지난 보관 주문 더보기 (Lazy Loading) 구현 (P40)

- **P40 (내 주문 확인 지난 보관 주문 더보기)**: 주방 실시간 조회 속도는 0.00초 딜레이로 유지하고, 이용자가 원할 때 `📁 지난 보관 주문 내역 불러오기` 버튼을 눌러 과거 보관된 주문 내역까지 불러오는 Lazy Loading 방식을 적용했습니다. 백엔드(`gas/40_Orders.gs`) 및 Mock API(`js/config.js`)에 `includeArchived` 옵션을 추가하고 `guest-orders.html` UI를 연동하였으며, `service-worker.js` 캐시 버전을 `kiosk-cache-v221`로 상향하여 실운영 배포 및 사용자 기기 수동 검증을 완료했습니다.

## 2026-07-20 - 지난 주문 보관(아카이빙) 명시적 확인 오류 분석 및 조치 (P31)

- **P31 (지난 주문 보관 명시적 확인 조치)**: 확인 문구 입력 시 공백이나 미세한 텍스트 차이로 보관이 거부되던 현상을 해결하기 위해 프론트엔드(`js/kitchen.js`) 및 백엔드(`gas/40_Orders.gs`)에 `.trim()` 및 다중 유연 승인 문구(`주문보관확인`, `지난주문보관` 등)를 적용하고, `service-worker.js` 캐시 버전을 `kiosk-cache-v220`으로 상향하여 수동 검증을 완료했습니다.

## 2026-07-20 - 직원 후기 관리 화면(reviews.html) CSS/JS 외부 파일 분리 리팩토링 (P33)

- **P33 (직원 후기 관리 화면 분리 리팩토링)**: 약 1,800라인 규모였던 `reviews.html` 파일 내부의 인라인 스타일(CSS) 및 인라인 스크립트(JS)를 `css/reviews.css` 및 `js/reviews.js` 외부 파일로 안전하게 분리하고 외부 참조 구조로 대체했습니다. 변경사항을 `service-worker.js` 캐시 목록에 등록하고 캐시 버전을 `kiosk-cache-v219`로 상향하여 수동 검증을 완료했습니다.

## 2026-07-20 - 게스트 주문 페이지(guest.html) CSS/JS 외부 파일 분리 리팩토링 (P32)

- **P32 (게스트 주문 페이지 분리 리팩토링)**: 약 2,010라인 규모였던 `guest.html` 파일 내부의 인라인 스타일(CSS) 및 인라인 스크립트(JS)를 `css/guest.css` 및 `js/guest.js` 외부 파일로 안전하게 분리하고 외부 참조 구조로 대체했습니다. 변경사항을 `service-worker.js` 캐시 목록에 등록하고 캐시 버전을 `kiosk-cache-v218`로 상향하여 수동 검증을 완료했습니다.

## 2026-07-20 - 주방 페이지(kitchen.html) CSS/JS 외부 파일 분리 리팩토링 (P30)

- **P30 (주방 페이지 분리 리팩토링)**: 약 3,160라인 규모였던 `kitchen.html` 파일 내부의 인라인 스타일(CSS) 및 인라인 스크립트(JS)를 `css/kitchen.css` 및 `js/kitchen.js` 외부 파일로 안전하게 분리하고 외부 참조 구조로 대체했습니다. 변경사항을 `service-worker.js` 캐시 목록에 등록하고 캐시 버전을 `kiosk-cache-v217`로 상향하여 수동 검증을 완료했습니다.

## 2026-07-20 - 관리자 페이지(admin.html) CSS/JS 외부 파일 분리 리팩토링 (P29)

- **P29 (관리자 페이지 분리 리팩토링)**: 약 4,170라인 규모였던 `admin.html` 파일 내부의 대규모 인라인 스타일(CSS) 및 인라인 스크립트(JS)를 `css/admin.css` 및 `js/admin.js` 외부 파일로 안전하게 분리하고 외부 참조 구조로 대체했습니다. 변경사항을 `service-worker.js` 캐시 목록에 등록하고 캐시 버전을 `kiosk-cache-v216`으로 상향하여 수동 검증을 완료했습니다.

## 2026-07-20 - 미사용 CSS 및 잔여 코드 정리 (P8)

- **P8 (미사용 CSS 및 잔여 코드 정리)**: 관리자(admin/reviews - Phase 1), 주방/전광판(kitchen/board - Phase 2), 손님용 주문 내역(guest-orders - Phase 3) 화면 전체에 걸쳐 사용되지 않던 레거시 CSS 클래스 및 JS 레거시 함수를 정밀 정적 분석을 통해 안전하게 제거하고 수동 검증 완료 및 `kiosk-cache-v215` 상향 반영.

## 2026-07-20 - 직원 답글 작성 시 2x2 이모티콘 그리드 UI 도입 및 화면 연동 (P28)

- **P28 (달곰이 감사 답글 이모티콘 연동)**: 발달장애인 직원의 답글 작성 UX를 위해 기존의 가로형 프리셋을 2x2 그리드 정사각형 이모티콘 카드로 대개편하고, 손님 내 주문 확인 감사 답글을 1:1 병렬(이모티콘+말풍선) 레이아웃으로 개선했으며, 메인 소통 롤링 피드(38px), 최근 후기 보드(32px) 및 상세 모달에 달곰이 답글 스티커를 유기적으로 연동하여 수동 검증 완료 및 `kiosk-cache-v213` 상향 반영.

## 2026-07-20 - 손님 감정 이모티콘 팩 적용 및 직원 후기 TTS 기능 연동 (P23, P26)

- **P23 (손님 후기 달곰이 이모티콘 팩)**: 손님이 후기 작성 시 선택할 수 있는 달곰이 캐릭터 감정 이모티콘 4종(`assets/dalgomi_*.png`)을 연동하고, 후기 리스트와 관리자 화면에 고화질 스티커로 노출되도록 반영했습니다.
- **P26 (직원용 후기 관리 TTS 읽어주기)**: `reviews.html` 내 개별 후기 카드에 음성 재생 버튼을 제공하고, SpeechSynthesis API를 활용한 낭독 및 다른 카드 클릭 시 중복 재생 차단 로직을 구현하여 반영했습니다.

## 2026-07-20 - 직원 후기 관리 AAC 프리셋 복구, TTS 연동 및 이모티콘 필터링 정제 (P27)

- **P27 (AAC 프리셋 복구 및 TTS 이모티콘 제거 낭독)**: Git 커밋 누락으로 인한 AAC 답글 즉시 저장, 테두리 강조, 락 제어 기능을 재구현하고 프리셋 클릭 및 후기 읽어주기(P26) TTS 재생 시 유니코드 이모티콘 텍스트를 자동으로 제거하여 본문만 정갈하게 낭독하도록 보강하여 수동 검증 완료 및 `kiosk-cache-v212` 상향 반영.

## 2026-07-19 - 이용 신청/WAITLIST/정원/인쇄/관리자 UI/마감 경계/후기 노출 및 클릭 분기 개선 (P13, P14, P16, P17, V1, P20, P21, P22, P25)

- **P25 (내 주문 내역 카드 클릭 분리)**: 수령완료 및 후기작성 완료 주문에 대해 `complete.html` 이동을 차단하고, 카드 클릭 시 배달왔삼 직원의 감사 답글 TTS 재생 및 시각적 파동 강조 효과를 적용하여 수동 검증 완료했습니다.
- **P22 (발달장애인 AAC 답글 작성 및 소통 피드 노출)**: AAC 이모티콘 칩 클릭 시 한국어 TTS 연동, 원터치 교체/락 제어, 메인 화면(`guest.html`) 롤링 피드 위젯 렌더링, `guest-orders.html` 1:1 감사 답글 노출 수동 검증 완료 및 `kiosk-cache-v204` 상향 반영.
- **P13 (이용 신청·승인 관리)**: GAS 배포 및 `setupGuestApplicationSheet()`를 실행하여 `이용신청` 22열 운영 시트 반영 및 승인 관리를 완료했습니다.
- **P14 (1차 시범 신청 정원 관리)**: 1~100명 조절 가능한 시범 신청 정원 설정 및 `PENDING + APPROVED` 정원 제어 로직을 GAS 배포 완료 및 검증했습니다.
- **P16 (관리자 인증 종료 버튼 개선)**: 세 관리 화면의 `🚪 나가기` 버튼, 툴팁, 모바일 배치를 반영하고 수동 검증 완료했습니다.
- **P17 (모바일 간식 순서 변경 UI)**: 모바일 간식 이동·경계 제어 및 분리 저장 실패 복귀 수동 검증을 완료했습니다.
- **V1 (마감 경계)**: 마감 5분 유예, 즉시 마감 및 읽기 전용 메뉴 전환 동작을 수동 검증 완료했습니다.
- **P20 (포장 주문용 체크리스트 분리 인쇄)**: `print-bills.html` 필터 토글 및 포장 체크리스트 렌더링 배포를 완료했습니다.
- **P21 (이용 신청 대기자 WAITLIST)**: `admin.html` 모달 UI 개선 및 P14 연동 대기자 관리 GAS 구현 및 배포를 완료했습니다.
- *(참고)* **P7 (후기 참여 정보 표시)**는 신규 후기 강화 기능(P22~P24)으로 대체됨에 따라 폐기 처리했습니다.

## 2026-07-16 - 제공완료 제외 옵션 및 라벨 인쇄 시작위치 지정 (P19)

- 빌지/라벨지 인쇄 화면 상단에 '제공 완료된 주문 제외' 옵션(localStorage 영구 저장)을 탑재하여 `servedYn === 'Y'` 상태 주문을 필터링하도록 구현하고 수동 검증했습니다.
- 인쇄 형식이 `애니라벨 V3050`일 때만 노출되는 3x7 미니 시각적 그리드 시작점 선택기를 구현하고, 첫 페이지의 오프셋 가상 빈 카드(`.blank-card`) 인쇄 밀어내기 로직을 작성하여 수동 검증했습니다.
- 품목 제외와 오프셋 밀어내기 시에도, 라벨 순번 지시(예: `아메리카노 (1/3)`)는 원래 전체 주문 수량 기준을 정확히 유지하도록 로직을 작성했습니다.
- 정적 캐시 명세를 `kiosk-cache-v200`으로 상향했습니다.

## 2026-07-16 - 라벨지 인쇄 개별 분할 및 정렬 보정 (P18)

- 라벨지 모드(`애니라벨 V3050`)일 때 개별 상품 수량(quantity) 단위로 라벨 카드가 분리 인쇄되고 `(순번/총수량)`이 표시되도록 구현하고 수동 검증했습니다.
- 인쇄 시 2열과 3열 라벨 카드가 1열 대비 우측으로 1mm 평행 이동하도록 CSS 정렬 오차 보정을 적용하고 수동 검증했습니다.
- 정적 캐시 명세를 `kiosk-cache-v199`로 상향했습니다.

## 2026-07-13 - 핸드오프 단일 기준 전환

- `handoff.md`를 현재 작업 상태의 유일한 기준으로 정하고 중복 상태 문서인 `active-work.md`를 아카이브했습니다.
- `verification.md`는 상태 없이 재사용하는 검증 절차로, `work-log.md`는 완료 작업 전용으로 역할을 분리했습니다.
- 작업 번호·상태·링크·캐시와 신청 시트 계약을 확인하는 `scripts/check-handoff.js`와 GitHub Actions 검사를 추가했습니다.

## 최근 완료 요약

- **2026-07-12**: 이용자 크레딧·간식 재고 인라인 게이지 편집과 15·30 상한을 수동 검증했습니다.
- **2026-07-11**: 게스트 운영 패널 모바일 재배치, 주방 요약 카드, 주문·통계·재고 탭과 후기 `더 보기`를 수동 검증했습니다.
- **2026-07-10**: 주문보관 안전 처리, 관리자 로그인 게이트, 관리 화면 탭·외부 화면 버튼과 주방 재고 표시를 수동 검증했습니다.

세부 변경 내역은 [2026-07-13 작업 로그 아카이브](work-log-archive-2026-07-13.md)와 [장기 핸드오프 아카이브](handoff-archive.md)에서 확인합니다.
