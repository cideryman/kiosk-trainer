# 배달왔삼 실시간 운영 상황판 전환 및 주문 정원 관리 구현 보고서 (P117)

## 1. 개요 및 배경

기존 '사전 이용신청 → 관리자 승인 → 지정일 주문' 방식의 번거로운 진입 장벽으로 인해 신청이 저조했던 문제를 해결하기 위해, 신청 절차를 전면 폐지하고 **'실시간 운영 상황판(소개 페이지) + 운영시간 내 즉시 주문(누구나 가능)'** 구조로 서비스 모델을 혁신적으로 전환했습니다.

동시에 현장 부담을 철저히 방지하기 위해 **총 주문 정원(기본 5건)**, **최대 배달 건수(기본 2건)**, 그리고 **배달 가능 지역(기본 영주시 동 지역)**을 관리자가 직접 조절할 수 있도록 구현하고, 배달 주문 시 수령인 확인 및 안전한 전달을 위한 **연락처(전화번호) 필수 수집**을 구축했습니다.

---

## 2. 주요 구현 내용

### 1) 관리자/주방 운영 설정 UI 및 GAS 백엔드
- **파일**: [`kitchen.html`](file:///c:/Users/user/Desktop/키오스크/kitchen.html), [`js/kitchen.js`](file:///c:/Users/user/Desktop/키오스크/js/kitchen.js), [`gas/60_Settings.gs`](file:///c:/Users/user/Desktop/키오스크/gas/60_Settings.gs), [`gas/40_Orders.gs`](file:///c:/Users/user/Desktop/키오스크/gas/40_Orders.gs), [`js/config.js`](file:///c:/Users/user/Desktop/키오스크/js/config.js)
- **주요 변경 사항**:
  - `kitchen.html`: 게스트 기본 운영 설정 섹션 내에 **총 주문 정원 (건)**, **최대 배달 건수 (건)**, **배달 가능 지역 안내** 입력 필드 추가.
  - `js/kitchen.js`: 설정 로드/수정/저장 흐름에 3개 필드 바인딩 및 변경 감지(dirty) 연동.
  - `gas/60_Settings.gs`:
    - `guestMaxOrderCount` (기본값 5), `guestMaxDeliveryCount` (기본값 2), `guestDeliveryArea` (기본값 '영주시 동 지역 (가흥동, 영주동, 휴천동 등)') 저장 지원.
    - `getGuestSettings` 호출 시 당일 접수된 유효 주문/배달 건수를 집계하여 `remainingOrderCount`, `remainingDeliveryCount`, `isOrderCapped`, `isDeliveryCapped` 실시간 산출 및 반환.
  - `gas/40_Orders.gs`:
    - 주문 제출 트랜잭션 내에서 당일 총 주문 수 초과 시 즉시 차단.
    - 배달 주문 시 당일 배달 한도 초과 시 차단 (포장만 허용).
    - 배달 주문 시 연락처(`deliveryPhone`) 및 상세 장소(`deliveryPlace`) 필수 검증.
    - **DB 스키마(24열 A:X) 보존**: 배달 연락처를 P열(`deliveryPlace`)에 `[상세주소] (연락처: 010-XXXX-XXXX)` 형식으로 결합 저장하여 주방/영수증/전광판 시스템과 완벽 호환.
  - `js/config.js`: 로컬 목업 환경에서도 실제 백엔드와 100% 동일하게 정원 집계, 슬롯 마감, 연락처 필수 검증 및 포맷 저장이 작동하도록 동기화.

---

### 2) 배달왔삼 소개 및 실시간 운영 상황판 개편
- **파일**: [`guest-apply.html`](file:///c:/Users/user/Desktop/키오스크/guest-apply.html), [`css/guest-apply.css`](file:///c:/Users/user/Desktop/키오스크/css/guest-apply.css), [`js/guest-apply.js`](file:///c:/Users/user/Desktop/키오스크/js/guest-apply.js)
- **주요 변경 사항**:
  - 복잡했던 사전 신청 폼을 전면 제거하고, 깔끔한 **실시간 운영 상황판(Live Board)**으로 전면 개편.
  - 실시간 상황판:
    - **오늘 주문 상태**: 주문 가능 / 준비 중 / 주문 마감 배지.
    - **남은 주문 수량**: 오늘 남은 주문 N건 실시간 카운트 (총 정원 대비).
    - **남은 배달 가능 건수**: 배달 가능 N건 (배달 정원 마감 시 '배달 마감 · 포장 가능'으로 표시).
    - **배달 가능 지역**: 관리자가 설정한 지역 안내 문구 동적 노출.
    - **즉시 주문하기 CTA**: 운영 시간 및 잔여 슬롯이 있을 때만 활성화, 마감 시 자동 비활성화.
  - 30초 주기로 `getGuestSettings` API를 백그라운드 폴링하여 새로고침 없이도 최신 슬롯 현황 갱신.
  - **달곰이 대원의 배달 & 주문 수칙 4가지** 및 스토리 섹션 제공 (정확한 주소/연락처, 따뜻한 환대, 시간 준수 등).

---

### 3) 주문서 화면 배달 연락처 수집 & 마감 제어
- **파일**: [`confirm.html`](file:///c:/Users/user/Desktop/키오스크/confirm.html), [`js/confirm.js`](file:///c:/Users/user/Desktop/키오스크/js/confirm.js)
- **주요 변경 사항**:
  - `confirm.html`:
    - 배달 버튼 하단에 **오늘 배달 정원 마감 알림 배너**(`delivery-capped-notice`) 배치.
    - 배달 선택 시 표시되는 배송지 영역에 **배달 가능 지역 안내 배너** 동적 연동.
    - **받는 분 연락처**(`delivery-phone-input`, type="tel") 필수 입력 필드 추가.
    - 약식 개인정보 파기 안내문 명시: "* 안전한 배달 및 수령 확인 목적으로만 수집되며, 배달 완료 후 즉시 파기됩니다."
  - `js/confirm.js`:
    - `refreshConfirmGuestSettings()` 및 `applyDeliveryCapStatus()`:
      - 오늘 배달 정원이 마감(`isDeliveryCapped === true`)된 경우, 배달 버튼을 비활성화(`disabled`, 반투명, '배달 (마감 🛵)')하고 마감 안내 배너 표시.
      - 이미 배달이 선택되어 있었다면 자동으로 포장(`pickup`) 모드로 안전하게 강제 전환.
    - `submitOrder()`:
      - 배달 선택 시 연락처 누락 여부를 확인하고 미입력 시 친절한 안내 후 포커스.
      - 멱등키 시그니처(`buildOrderAttemptSignature`) 및 주문 페이로드에 `deliveryPhone` 정상 반영.

---

### 4) 공개 네비게이션 탭 및 진입 동기화
- **파일**: [`guest.html`](file:///c:/Users/user/Desktop/키오스크/guest.html), [`js/guest.js`](file:///c:/Users/user/Desktop/키오스크/js/guest.js), [`reviews-public.html`](file:///c:/Users/user/Desktop/키오스크/reviews-public.html), [`service-worker.js`](file:///c:/Users/user/Desktop/키오스크/service-worker.js)
- **주요 변경 사항**:
  - 상단 공개 탭 라벨을 `이용신청` ➔ `이용안내`로 전 화면 통일.
  - `guest.html`의 하단 플로팅 배너 문구를 `배달왔삼 이용안내 (안내 보기 ›)`로 전환.
  - `js/guest.js`: 오늘 총 주문 정원이 마감(`isOrderCapped === true`)된 경우, '오늘 주문 정원(N건)이 모두 마감되었습니다' 친절한 마감 화면 자동 표시.
  - 정적 캐시 갱신을 위해 서비스워커 버전을 `kiosk-cache-v357`로 상향.

---

## 3. 검증 결과

### 1) 단위 및 통합 자동화 검증
- **핸드오프 및 계약 검증 (`scripts/check-handoff.js`)**: **PASS** (44개 ID, 캐시 `kiosk-cache-v357`, 이용신청 22열 스키마 유지 확인)
- **기존 일정 및 엔진 검증 (`scripts/test-guest-schedule.js`)**: **PASS** (정기, 추가, 긴급 운영 모두 정상)
- **API 보안 경계 검증 (`scripts/test-api-input-security.js`)**: **PASS**
- **데이터 안전 검증 (`scripts/test-data-safety.js`)**: **PASS**
- **기존 키오스크 기능 검증 (`scripts/test-kiosk-order-menu-display.js`)**: **PASS**
- **신규 기능 7대 도메인 자체 검증 스크립트**: **ALL PASS** (Settings GAS, Orders GAS, confirm UI/JS, kitchen UI/JS, guest-apply UI/JS, config mock 전 영역 무결성 확인)

---

## 4. 운영자 매뉴얼 및 GAS 배포 안내

> [!IMPORTANT]
> **사용자 GAS 편집기 반영 안내**
> 이번 변경사항을 실제 구글 시트 및 운영 서버에 적용하시려면 Apps Script 웹 편집기에서 아래 2개 파일의 내용을 덮어씌운 뒤 새 버전으로 웹앱 배포를 진행해 주세요.
> 1. `gas/60_Settings.gs`
> 2. `gas/40_Orders.gs`

### 주방에서 설정하는 방법
1. 주방 화면(`kitchen.html`) 우측 상단의 **운영 설정**을 엽니다.
2. **배달왔삼 기본 설정** 영역에서:
   - **총 주문 정원**: 원하는 당일 주문 건수(예: 5건) 입력
   - **최대 배달 건수**: 감당 가능한 배달 건수(예: 2건) 입력
   - **배달 가능 지역**: 배달 가능한 지역(예: "영주시 가흥동, 영주동 일대") 입력
3. **설정 저장** 버튼을 누르면 즉시 실시간 상황판과 주문서에 반영됩니다.
