# 배달왔삼 키오스크 프로젝트 핸드오프

> 마지막 갱신: 2026-08-27

## 현재 상태

- **최근 이슈**: 주방 `지난 주문 보관`의 `보관 점검`이 24열과 23열의 차이를 전체 불일치로 판정해 실제 보관을 막을 수 있던 문제가 P92에서 수정되었습니다. `주문보관`의 기존 A:W 구조는 보존하고, X열 `commitStatus`는 새 `주문내역`에만 유지합니다.
- **운영 DB 기준**: `주간보호 매점DB26.8.22.xlsx`의 기존 15개 시트와 1행 헤더를 대조했고, P93 배포 후 `이용운영기록` 9열 시트가 추가되어 현재 운영 기준은 16개 시트입니다. 운영 시트 14개와 `주문보관_자동백업_*` 백업 시트 2개가 있으며, `주문내역`은 24열(A:X), `주문보관`과 자동 백업은 23열(A:W)입니다.
- **이용신청 DB 계약**: `이용신청` 22열(A:V) 구조를 유지합니다.
- **P93 시트 변경**: `setupGuestApplicationOperationsSheet()` 실행으로 개인정보 없이 주차 운영 이력을 저장하는 `이용운영기록` 9열 시트가 생성됩니다. `이메일알림큐`는 기존 A:J 뒤에 `notificationType`, `referenceId`를 추가해 A:L 12열로 확장되며 기존 주문 행은 하위 호환됩니다.
- **이용신청 계약**: `guestApplicationCapacity`는 1~100명 범위에서 조절하는 주당 운영 안내 인원일 뿐 신청·승인·주간 배정 제한이 아닙니다. 신규 신청은 정원과 관계없이 `PENDING`으로 접수하고, `WAITLIST`는 관리자가 직접 보류할 때만 사용합니다.
- **GAS 구조**: `gas/*.gs` 기능별 분리를 완료했으며 같은 Apps Script 프로젝트 안에서 함께 배포합니다.
- **GAS 반영 책임**: 회사·가정 환경의 Google 계정과 프로젝트가 섞이지 않도록 로컬 자동 인증·설정·배포를 사용하지 않습니다. `clasp` 설정 파일을 만들거나 로그인·푸시·배포를 실행하지 않으며, GAS 소스 반영·새 버전 배포·일회성 함수 실행은 사용자가 Apps Script 편집기에서 직접 수행합니다.
- **API·정적 캐시**: P108 GAS API `2026-08-27.3`은 2026-08-27 사용자 배포 확인을 완료했습니다. P109 구현 코드는 API `2026-08-27.4`, 서비스워커 `kiosk-cache-v350`이며 새 GAS·정적 배포 대기입니다.
- **이용 안내**: 문자 안내에서 연결하는 `order-guide.html`과 `review-guide.html`에 실제 화면을 활용한 주문방법·후기 작성방법을 추가하고, 관리자 사이드바에서 이용신청 페이지와 안내 화면을 각각 열 수 있게 했습니다(P81).
- **안내 화면 구분 개선**: 주문·후기 안내의 설명을 페이지별 강조색 패널로 묶고, 스크린샷을 `실제 화면` 배지가 있는 중립 프레임으로 분리했습니다(P82).
- **안내 화면 이용 흐름 보완**: 안내 탭 sticky 동작, 직접 접속 PWA 등록, 상단 주문 바로가기와 후기 작성 직행, 가이드 이미지 정적 검사를 추가했습니다(P83).
- **이용 신청 상태 문구 개선**: 공개 화면에서 잔여·대기 인원 숫자를 제거하고 일반 신청·대기 신청·마감·조회 오류를 행동 중심 문구와 버튼으로 통일했습니다(P84).
- **레시피 가이드 연동 결정**: `recipe-guide`는 주문 시스템과 분리된 저장소·배포로 유지하고, 배달왔삼 주방에서 외부 바로가기로 연결합니다. 레시피 파일을 이 저장소에 복사하거나 iframe으로 삽입하지 않으며, 이후 간식별 직접 연결은 `?recipe=` 주소와 명시적인 레시피 ID 매핑으로 확장합니다(P85).
- **최근 구현**: **주문자 후기 조회·7일 수정·사진 유지/교체/삭제·수정 이력 표시(P91)**를 구현했습니다. P90 랜덤 표시명 비활성은 운영 페이지에서 정상 동작을 확인했습니다.
- **수요일 정기 운영(P99 완료)**: 2026-08-26 실제 운영에서 13:00 자동 개방, 15:00 자연 마감과 공개 주문 화면의 마감 화면 자동 전환을 확인했습니다. 마감 직전 주문이 없어 5분 완료 유예는 이번 회차에서 실행하지 못했으나 핵심 정기 운영 흐름은 정상입니다.
- **배달왔삼 주문 이메일(P100 완료)**: 관련 GAS 3개 파일 반영과 웹앱 배포 후 실제 배달왔삼 주문 이메일 수신을 확인했습니다. 배포 전에 쌓인 대기 큐는 확인할 대상 행이 없어 별도 재현하지 않았습니다.
- **가변·추가 운영 일정(P102 완료)**: 날짜 지정 추가 운영·일정 수정·취소·종료 시각 긴급 운영·PC/모바일 안정성에 이어 2026-08-26 새 일정 엔진의 수요일 13:00 자동 개방, 15:00 자연 마감과 공개 화면 전환까지 확인했습니다.
- **키오스크 주문 문구 분리(P101 완료)**: 일반 키오스크 확인 화면은 달곰이·배달왔삼 소개 카드를 숨기고 주문 버튼을 `주문 완료하기`로 표시하며, 완료 화면은 접수 중심 문구를 사용합니다. 배달왔삼의 온기 전달 카드·포장/배달 선택·실시간 추적은 유지하며 GitHub Pages와 서비스워커 `kiosk-cache-v343` 반영을 확인했습니다.
- **일반 키오스크 1회 주문 한도(P103 완료)**: 일반 이용자의 기존 `credit` 값을 충전식 잔액이 아닌 개인별 1회 주문 한도로 전환했습니다. 운영 API `2026-08-25.2`, 활성 이용자 한도 10, 반복 주문 후 한도 유지, 합계 10 허용·11 차단, 일반 취소 시 재고만 복구를 실제 기기에서 확인했습니다. 배달왔삼은 기존 온기 차감과 취소 환불이 유지되며, GitHub Pages와 서비스워커 `kiosk-cache-v344`도 반영됐습니다.
- **개인정보·주문 데이터 안전(P104 완료)**: 이용신청의 비연속 행 덮어쓰기를 제거하고 익명화 임시 백업·검증·자동 복구를 추가했습니다. 지난 주문 보관은 서버 재점검, 주문내역·주문보관 이중 백업, 보관 검증 후 과거 행만 삭제, 실패 시 양쪽 복구 방식으로 변경했습니다.
- **주문 취소·환불 안전(P105 완료)**: 관리자·이용자 취소를 하나의 잠금 트랜잭션으로 통합하고 주문·간식·게스트 온기를 임시 백업한 뒤 배치 저장·재조회 검증합니다. 실패하면 전부 자동 복구하며, 신규 일반·게스트 주문 모두 UUID 기반 토큰을 사용합니다.
- **주문·이메일 큐 성능(P106 완료)**: `placeOrder` 선택형 단계 계측, 일반 이용자 캐시 재사용, 커밋 뒤 주문 잠금 해제, 이메일 큐의 주문 중복 스캔 생략·스키마/수신자 캐시·상태 열 부분 쓰기를 구현했습니다. 운영 DB 강제 실패 검증 없이 자동검사와 실제 주문 관찰로 확인합니다.
- **신청관리 원자성(P98·P94)**: 운영 완료·확정 취소는 중복 제거한 요청 ID 전부를 먼저 조회·검증하고, 하나라도 누락되거나 처리 불가능하면 로그·결과 캐시·시트 변경 없이 전체 거부합니다. 기존 일괄 상태 변경과 요청 멱등성, P98 화면 구조는 유지합니다.
- **핸드오프 기준선(P107 완료)**: P80·P96·P100~P106의 완료 상태를 정리하고, 미완료 P93 기록을 완료 로그에서 아카이브로 이동했습니다. 이후 새 작업은 P108부터 부여합니다.
- **후속 안전 과제**: P108 주문 소유권·공개 API 입력 방어는 구현·자동검사와 GAS 배포를 완료했고, P109 자동복구 실패 운영 가시성도 구현·자동검사를 완료해 배포 대기입니다. P110 카카오 이용자 증명·요청 남용 방어는 검토 대기이며 P104~P106 완료 범위는 다시 열지 않습니다.
- **외부 상태 주의**: 저장소에서는 GAS 실제 배포 여부와 운영 시트 생성 여부를 확인할 수 없습니다. 사용자 확인 전에는 항상 `배포·운영 검증 대기`로 기록합니다.
- **프로포절 기능 검토**: 사회복지공동모금회 지원사업 계획에 필요한 운영회차·역할수행·성과평가·다회용 컵·협력기관 관리 범위를 `docs/proposal-feature-review.md`에 정리했습니다. 구현은 기존 주문 흐름과 분리된 사업관리 계층으로 진행합니다.

## 현재 우선순위

이 표가 **현재 작업 상태의 유일한 기준**입니다. 상태를 다른 문서에 반복해서 기록하지 않습니다.

| 순서 | ID | 작업 | 상태 | 다음 행동 | 검증 절차 |
|---:|---|---|---|---|---|
| 1 | P108 | 주문 소유권·API 입력 보안 마무리 | 배포 완료·수동 검증 대기 | GAS `2026-08-27.3` 사용자 배포와 저장소 푸시 완료. 실제 화면에서 새 토큰 계약과 `ALLOW_LEGACY_ADMIN_GET` 속성 삭제 여부 확인 | 토큰 상태 조회·취소·후기·사진, 관리자 GET/헤더 API 차단, 행사명 고정 서식과 간식 숫자 경계를 실제 화면에서 확인 |
| 2 | P109 | 취소 자동복구 실패 운영 가시성 | 구현 완료·배포 대기 | GAS `2026-08-27.4`와 정적 파일 `kiosk-cache-v350` 배포 후 관리자 운영점검의 자동복구 경고 영역 확인 | staging 실패 주입에서 새 세션에도 주문번호·단계·백업명만 남고 중복 경고는 합쳐지며, 수동 조치 확인 완료 후 열린 경고에서 사라지는지 확인 |
| 3 | P110 | 카카오 이용자 증명·공개 요청 남용 방어 | 검토 대기 | 서버 서명 카카오 증명 토큰, 임의 `guestKey` 보너스 차단, 기존 로그인 세션의 점진적 전환과 공개 신청·주문 API의 제한 정책을 별도 계획으로 확정 | 위조·재사용·다른 이용자의 증명과 임의 `guestKey`가 거부되고 정상 카카오 로그인·주문·조회·후기 흐름 및 일반적인 재시도가 유지되는지 확인 |
| 1 | P98 | 신청관리 일괄 처리·상태 그룹화·신청서 단순화 | 배포 완료·수동 검증 대기 | 별도 강제 시험 없이 실제 신청관리 사용에서 부분·중복 처리 오류만 관찰 | 자동검사 완료. 실제 승인·반려·중지·연락·운영 확정·완료·취소에서 부분 처리와 알 수 없는 API 오류가 없는지 확인 |
| 1 | P93 | 이용신청 주간 수동 운영·일시 중단·신청 알림 | 배포 완료·수동 검증 대기 | 2026-08-26 주간 배정·일괄 완료·중복 방지·중단 후 순번 보존은 이상 없음을 확인했습니다. 신규 이용신청 시 신청 저장 응답과 신청 알림 이메일만 다음 실제 신청에서 확인 | 확인 완료: 수동 운영 핵심 흐름. 남은 검증: 신청 저장 즉시 응답·신청 이메일 비동기 발송 |
| 1 | P94 | 주간 운영 단일 체크리스트·확정 취소·서비스 순서 정리 | 배포 완료·수동 검증 대기 | P98의 기반 기능으로 함께 배포 완료했으며 별도 강제 시험 없이 실제 사용을 관찰 | P98과 함께 실제 사용에서 확정 취소 후 재선택, 완료 대상 이동, 혼합 요청 전체 거부를 관찰 |
| 2 | P89 | GAS 주문 속도·설정 안정성·이메일 비동기 큐 개선 | 배포 완료·수동 검증 대기 | 2026-08-26 실제 주문과 이메일 수신은 정상이나 주문 완료가 가끔 오래 걸린다는 관찰이 있어 속도는 계속 관찰합니다. 빈 설정 저장은 설정의 선택 입력값을 비우고 저장할 때 빈 값이 유지되는지 확인하는 개발 회귀 항목입니다 | 실제 주문·이메일 수신 정상. 남은 검증: 간헐적 지연 관찰, 빈 선택 설정 저장, staging rollback·동시 주문 검사 |
| 1 | P87 | 주방 운영설정 불리언 설정값(랜덤 표시명/중복 주문) 저장 후 재체크 버그 수정 및 세그먼트 버튼 개편 | 구현 완료·배포 대기 | GitHub Pages 반영 후 랜덤 닉네임 비활성 상태에서 게스트·확인 화면 버튼 숨김 확인 | 주방 설정에서 랜덤 닉네임 활성/비활성 세그먼트 전환 및 저장 후 재조회 시 비활성 상태 유지, 게스트·확인 화면 버튼 숨김 확인 |
| 1 | P88 | 새 주문 이메일 알림 기능 ON/OFF 설정 스위치 도입 및 주문 응답 속도 최적화 | 구현 완료·배포 대기 | GAS 새 버전과 GitHub Pages 반영 후 이메일 알림 OFF 저장 및 주문 완료 속도 확인 | 알림 ON/OFF 스위치 전환 후 주문 완료 속도(OFF 시 0.5초 이내 완료) 및 ON 시 메일 발송 정상 동작 확인 |
| 1 | P82 | 주문·후기 안내 설명과 실제 화면 구분 개선 | 구현 완료·배포 대기 | GitHub Pages 반영 후 주문·후기 안내를 모바일·데스크톱에서 수동 확인 | 청록·주황 설명 패널과 회색 실제 화면 프레임이 구분되고, 320·375·430·720px에서 가로 넘침·글자 잘림 없이 기존 링크가 동작하는지 확인 |
| 1 | P83 | 주문·후기 가이드 접근성·PWA·이동 흐름 보완 | 구현 완료·배포 대기 | GitHub Pages 반영 후 직접 접속·상단 고정·주문 및 후기 이동을 수동 확인 | PC·390px·320px에서 탭 고정, 직접 접속 서비스 워커 등록, 주문 시작 및 후기 작성 경로와 빈 주문 안내 확인 |
| 1 | P84 | 이용 신청 상태·정원 안내 문구 개선 | 구현 완료·배포 대기 | GitHub Pages 반영 후 일반 신청·대기 신청·마감 상태를 수동 확인 | 공개 화면에 잔여·대기 인원 숫자가 없고 상태·상단 버튼·제출 버튼이 같은 신청 모드를 안내하는지 PC·390px·320px에서 확인 |
| 1 | P72 | 시스템 안정성·API 보안 경계·재사용 점검 도구 | 배포 완료·수동 검증 대기 | 운영 읽기 34/34 통과. staging `full` 검사와 24시간 관찰은 운영 규모·대규모 GAS 변경 시 재개하도록 보류 | 운영 읽기·보안 경계 34/34 통과, PWA·카카오 수동 확인. staging 쓰기·24시간 관찰은 보류 |
| 2 | P73 | 주방 준비 보드 시각자료·선택형 TTS | 구현 완료·배포 대기 | GitHub Pages 반영 후 이미지·수량·선택형 TTS와 기존 주방 주문 흐름을 함께 수동 검증 | `지금 꺼내올 간식`에 썸네일·간식명·총수량이 함께 보이고, 이미지 실패 시 텍스트 대체, 선택형 TTS 중복 재생 방지, PC·390px·320px 확인 |
| 3 | P74 | 배송지 안전 정규화와 빌지 체크리스트 묶음 기준 | 구현 완료·배포 대기 | GAS 새 버전 배포 후 배송지 별칭 시트 생성·저장·조회와 공백 정규화를 확인 | 별칭 충돌 차단, 예시 배송지 미자동 등록, 기존 주문 원문 보존, 관리자 인증과 빈 시트 시작 확인 |
| 4 | P75 | 주방 배달 범주 배송지별 묶음 표시 | 구현 완료·배포 대기 | P74 배포 후 GitHub Pages 반영과 주방·빌지 그룹 수동 검증 | 최초 주문 순서, 같은 배송지 재등장 그룹 합류, 다른 장소 미병합, 제공·인쇄 회귀 확인 |
| 5 | P76 | 주문표시명 자동 생성 허용 설정 | 구현 완료·배포 대기 | GAS 새 버전 배포 후 주방 설정에서 허용·비허용을 저장하고 게스트·주문확인 화면을 검증 | 허용 시 랜덤 이름 버튼 표시, 비허용 시 버튼 숨김·직접 입력 유지, 로컬 저장·카카오·완료·조회·취소·후기 회귀 확인 |
| 7 | P78 | 후기·간식 TTS 특수기호 정리 | 구현 완료·배포 대기 | GitHub Pages 반영 후 후기·간식 읽어주기에서 기호가 발화되지 않는지 수동 검증 | 이모지·따옴표·반복 물결표·연속 문장부호를 정리하되 화면 원문은 유지하고, 한국어 문장이 자연스럽게 읽히는지 확인 |
| 8 | P79 | 주방 오늘의 운영 결과 모달 집계 오류 수정 | 구현 완료·배포 대기 | GitHub Pages 반영 후 운영 결과 집계·복사 기능을 수동 검증 | 운영 결과 버튼에서 오류 없이 주문·온기·후기·태그 요약이 표시되고, 결과 복사와 모바일 모달 레이아웃이 정상인지 확인 |
| 8 | P71 | 공개 화면 API 로딩 복원력 및 최근 후기 캐시 최적화 | 구현 완료·배포 대기 | GAS 새 버전과 GitHub Pages를 반영한 뒤 공개 화면을 차례로 재검증 | 키오스크 이용자·간식, 배달왔삼 운영 설정·최근 후기, 신청 안내, 공개 후기, 전광판이 콜드 스타트와 일시적 404 후 정상 표시되는지 확인 |
| 8 | P70 | 관리자 화면 앱 업데이트 자동 확인·수동 점검 도구 | 구현 완료·배포 대기 | GitHub Pages 반영 후 관리자·주방·후기 사이드바에서 앱 업데이트 확인 실행 | 진입·재활성화·1시간 간격 자동 확인, 수동 최신 상태 안내, 새 버전 적용 시 자동 재로딩 확인 |
| 9 | P69 | 관리자 후기·신청·주방 읽기 API 콜드 스타트 내성 보강 | 구현 완료·배포 대기 | GitHub Pages 반영 후 후기·신청 관리·주방을 차례로 처음 열어 조회 확인 | 콜드 스타트에서도 40초 이내 응답, 일시적 네트워크 실패 시 1회 재시도, 후기 화면에 실제 GAS 오류 문구 표시 확인 |
| 10 | P67 | 관리자 로그인·주방 상태 변경·행사모드 변경 시 API 연결 실패 보강 | 구현 완료·배포 대기 | GitHub Pages 반영 후 관리자 재로그인, GAS 응답 지연 시 1회 재시도와 40초 쓰기 요청 확인 | 관리자 로그인 후 주문 제공 완료 및 행사모드 변경이 성공하고, 잘못된 키는 즉시 권한 오류로 구분되는지 확인 |
| 11 | P68 | `skipGuestApplicationWeek` API 라우터·관리자 인증 목록 누락 수정 | 구현 완료·배포 대기 | GAS 새 버전 배포 후 관리자 신청 상세에서 이번 주 건너뛰기 실행 | APPROVED·WAITLIST 신청의 `skipUntil` 설정 및 알 수 없는 액션 오류 미발생 확인 |
| 12 | P34 | 주문 확인 화면(confirm.html) CSS/JS 외부 파일 분리 리팩토링 | 구현 완료·배포 대기 | GitHub Pages 반영 후 주문 확인 화면 최종 확인 | `confirm.html` 로드, 수량 변경·삭제 및 주문 완료 흐름 정상 동작 확인 |
| 13 | P35 | 주문 완료 화면(complete.html) CSS/JS 외부 파일 분리 리팩토링 | 구현 완료·배포 대기 | GitHub Pages 반영 후 주문 완료 화면 최종 확인 | `complete.html` 외부 파일 로드, 완료 정보 표시·취소 확인창·자동 복귀 정상 동작 확인 |
| 14 | P36 | 메뉴 선택 화면(menu.html) JS 외부 파일 분리 리팩토링 | 구현 완료·배포 대기 | GitHub Pages 반영 후 메뉴 선택 화면 최종 확인 | `menu.html` 외부 파일 로드, 간식 선택·합계 갱신·주문 확인 이동 정상 동작 확인 |
| 15 | P37 | 내 주문 내역 화면(guest-orders.html) CSS/JS 외부 파일 분리 리팩토링 | 구현 완료·배포 대기 | GitHub Pages 반영 후 내 주문 확인 화면 최종 확인 | 외부 파일 로드, 주문 내역·상태 표시, 후기 모달 및 주문 상세 이동 정상 동작 확인 |
| 16 | P38 | 이용 신청 화면(guest-apply.html) CSS/JS 외부 파일 분리 리팩토링 | 구현 완료·배포 대기 | GitHub Pages 반영 후 이용 신청 화면 최종 확인 | 외부 파일 로드, 필수 입력·동의 및 로컬 목업 신청 성공 화면 정상 동작 확인 |
| 17 | P39 | 현황판 화면(board.html) CSS/JS 외부 파일 분리 리팩토링 | 구현 완료·배포 대기 | GitHub Pages 반영 후 실시간 간식 호출 전광판 최종 확인 | 외부 파일 로드, 주문 상태 분류·취소 제외 및 10초 자동 갱신 정상 동작 확인 |
| 18 | P59 | 공개 서비스 탭 헤더 통합(이용신청·고객 후기·주문하기) | 구현 완료·배포 대기 | GitHub Pages 반영 후 세 공개 화면 헤더 최종 확인 | 데스크톱·모바일 크기 일치, sticky 동작, 활성 탭 및 화면 이동 정상 확인 |
| 19 | P60 | 공개 서비스 헤더 브랜드 영역 확장 | 구현 완료·배포 대기 | GitHub Pages 반영 후 일반·행사 모드 헤더 최종 확인 | 일반·행사 모드에서 데스크톱·390px·320px 헤더 크기, 말줄임, 탭 위치 및 접근성 확인 |
| 20 | P61 | 배달왔삼 이름 이야기 모달 | 구현 완료·배포 대기 | GitHub Pages 반영 후 세 공개 화면의 브랜드 모달 최종 확인 | 로고 버튼, 데스크톱 중앙·모바일 하단 배치, 닫기·바깥 클릭·Esc·포커스 복귀 및 가로 넘침 확인 |
| 21 | P62 | 이용신청 화면 정보 구조와 폼 가독성 개선 | 구현 완료·배포 대기 | GitHub Pages 반영 후 이용신청 화면 최종 확인 | 히어로 균형, 요약 정보 재사용, 폼 구획, 개인정보 안내와 신청·대기·마감 상태 확인 |
| 22 | P63 | 공개 후기 민감 닉네임 중립 호칭 처리 | 구현 완료·배포 대기 | GAS `@120` 반영 완료, GitHub Pages 반영 후 공개 후기 화면 최종 확인 | 원본·관리자 이름 유지, 공개 API·화면에서 민감 표현이 고정 중립 호칭으로 표시되는지 확인 |
| 23 | P64 | 공개 후기 탐색성과 모바일 가독성 개선 | 구현 완료·배포 대기 | GitHub Pages 반영 후 후기 필터·더보기·모바일 화면 최종 확인 | 전체·사진·답글 개수와 12개씩 더보기, 카드 정보 순서, 태그 축약, 달곰이 단일 엠블럼, 320px 눈금 및 하단 버튼 확인 |
| 24 | P65 | 후기 사진 닉네임 공개 확인 UX | 구현 완료·배포 대기 | GitHub Pages 반영 후 실제 휴대폰 사진 1장으로 사용자 수동 확인 | 안내문·확대 미리보기·공개 확인 체크, 미확인 제출 차단, 비공개 전환·사진 삭제 시 초기화 확인 |
| 25 | P66 | 프로포절 실행용 사업관리·성과관리 기능 | 검토 대기 | `docs/proposal-feature-review.md`의 1단계 범위와 평가 기준을 확정한 뒤 데이터 계약 설계 | 운영회차·역할배정·4단계 관찰·익명 참여 ID·기본 KPI가 기존 주문 흐름을 방해하지 않는지 검토 |

### P99 구현 범위

- `운영설정`에 정기 운영 ON/OFF, 시작·종료 시각, 중단 수요일 날짜를 세로형 키로 저장하며 기본값은 OFF·13:00~15:00입니다.
- `gas/61_GuestSchedule.gs`의 순수 판정 함수가 Asia/Seoul 기준 수요일 회차, 수동 운영, 행사 모드 억제, 중단일, 다음 개방·상태 변경 시각을 계산합니다. 별도 시간 트리거는 만들지 않습니다.
- 일반 키오스크에는 영향을 주지 않고 `placeOrder`가 서버 판정 결과를 최종 확인합니다. 15시 자연 마감은 시작 주문에 5분 유예를 주며 회차 중단·즉시 마감에는 유예를 주지 않습니다.
- 주방은 정기 일정 저장, 날짜가 표시된 회차 중단·재개, 현재 운영 출처와 다음 자동 운영을 표시합니다. 중단 회차에는 수동 개방을 막고, 자동 운영 중 `지금 마감`은 해당 날짜만 중단한 뒤 다음 수요일에 재개합니다.
- 행사 모드에서는 자동 개방하지 않고 수동 운영은 유지합니다. 정기 운영과 수동 운영이 겹치면 더 늦은 마감까지 운영합니다.
- 보호자 화면은 서버가 반환한 다음 상태 변경 시각에 설정을 다시 조회해 13시 이전부터 열어 둔 페이지도 새로고침 없이 주문 가능 상태로 전환합니다.
- `scripts/test-guest-schedule.js`에서 시간 경계·유예·중단·다음 주 복귀·행사 억제·수동 중첩을 고정 시각으로 검사하고, staging 안정성 검사는 정기 설정을 백업·복원합니다.
- 공휴일 자동 판별과 개방 전 이메일·휴대전화 알림은 이번 범위에서 제외합니다.

### P100 구현 범위

- P88·P89에서 도입한 `adminOrderEmailNotificationEnabled`는 현재 일반 키오스크와 배달왔삼 주문에 공통 적용되므로, 키오스크 주문도 이메일 큐에 등록될 수 있습니다.
- `placeOrder`에서 서버가 이미 계산한 `isGuest`를 기준으로 배달왔삼 주문만 `enqueueOrderNotification`에 전달합니다. 프론트에서 보낸 배달 방식만으로 판별하지 않습니다.
- 기존 설정 키는 배포 호환성을 위해 유지하되 주방 화면 라벨을 `배달왔삼 새 주문 이메일 알림`으로 바꾸고, ON/OFF가 배달왔삼 포장·배달 주문에만 적용된다는 설명을 표시합니다.
- 일반 키오스크 주문은 주문 저장·재고·주방 표시를 그대로 수행하되 이메일 큐 행을 만들지 않습니다. P103부터 일반 이용자 한도는 영구 차감하지 않으며 기존에 큐에 들어간 행은 임의 삭제하지 않습니다.
- 배달왔삼 포장과 배달 메일 제목·본문은 기존 형식을 유지하고, 같은 주문번호의 중복 큐 방지와 비동기 재시도 동작을 보존합니다.
- API 계약은 `2026-08-24.2`, 서비스워커 캐시는 `kiosk-cache-v341`로 갱신하며 `scripts/test-order-email-routing.js`로 주문 유형·ON/OFF·중복 큐 판정을 검사합니다.

### P101 구현 범위

- `confirm.html`은 일반 키오스크와 배달왔삼이 공유하므로 현재 키오스크에도 `달곰이 대원에게 따뜻한 온기 한 조각을 전해요` 카드와 `온기 한 조각 담아 주문하기` 버튼이 표시됩니다.
- 일반 키오스크에서는 달곰이 대원·배달왔삼 소개 카드를 숨기고 주문 버튼을 `주문 완료하기`로 표시합니다. 완료 안내는 `주문이 잘 접수되었어요`처럼 키오스크 훈련 흐름에 맞춥니다.
- 배달왔삼에서는 기존 온기 전달 문구, 달곰이 대원 소개, 포장·배달 선택과 실시간 주문 추적을 그대로 유지합니다.
- P101 배포 당시에는 `사용한 온기`·`남은 온기`를 유지했으며, P103에서 일반 키오스크 문구를 `1회 주문 한도`·`이번 주문에서 남음`으로 후속 전환합니다.
- 화면 분기는 URL이나 화면 문구가 아니라 선택 이용자의 `userId === 'guest'` 여부를 사용하고, 세션 누락 시 기존 안전 복귀 동작을 보존합니다.

### P102 구현 범위

- 운영 구조는 `정기 운영 1개 + 날짜 지정 추가 운영`으로 단순화합니다. 정기 운영은 관리자가 요일과 시작·종료 시각을 바꿀 수 있고, 기존 수요일 13:00~15:00 설정을 그대로 승계합니다.
- 신청이 있을 때만 운영하는 목요일은 매주 반복 일정으로 자동 개방하지 않고, 주방에서 특정 날짜와 09:00~12:00 같은 시작·종료 시각을 등록·취소하는 추가 운영으로 관리합니다. 날짜가 지난 추가 일정은 자동으로 효력을 잃습니다.
- 보호자 화면은 `매주 수요일에 주문할 수 있습니다` 같은 고정 문구 대신 서버가 계산한 가장 가까운 확정 운영의 날짜·요일·시간을 표시합니다. 운영 중에는 현재 마감 시각을 우선 안내합니다.
- 주방의 `20분·30분·1시간 운영`과 분 단위 직접 설정은 제거합니다. 예외 상황은 `지금부터 운영`에서 종료 시각을 선택해 시작하고, `지금 마감`은 유지합니다.
- 정기 회차 중단·재개, 즉시 마감, 자연 마감 5분 완료 유예, 행사 모드의 자동 개방 억제, 수동 운영과 일정 운영의 더 늦은 마감 우선 규칙은 유지합니다.
- 설정 저장·운영 판정·주문 최종 검증·로컬 Mock이 같은 일정 계약을 사용하고, PC·390px·320px에서 날짜·요일·시간 입력과 등록·취소·긴급 운영 버튼을 확인합니다.
- 추가 일정은 날짜당 1개만 허용하고 목록 행에서 바로 수정합니다. 정기 회차 중단은 그 날짜의 긴급 운영만 함께 막으며 별도로 등록한 추가 일정은 유지합니다.
- API 계약은 `2026-08-25.1`, 서비스워커 캐시는 `kiosk-cache-v342`로 갱신합니다.

### P103 구현 범위

- `이용자목록` C열 `크레딧`과 API의 `credit` 필드는 호환성을 위해 유지하되 일반 키오스크의 개인별 1회 주문 한도로 해석합니다. 범위는 1~15, 신규 기본값은 10입니다.
- 일반 주문은 주문 합계가 개인 한도를 넘는지 서버에서 검증하지만 이용자 시트 값을 차감하지 않습니다. 응답의 `beforeCredit`은 한도, `afterCredit`은 한도에서 이번 주문 합계를 뺀 화면용 값입니다.
- 일반 주문 취소는 주문 상태와 재고만 복구하며 한도는 변경하지 않습니다. 배달왔삼은 기존 게스트 지갑 차감·취소 환불·배달비·보너스 온기를 그대로 유지합니다.
- 이용자 선택·메뉴·확인·완료·관리자 화면은 일반 흐름에서 `1회 주문 한도`를 사용하고 배달왔삼 화면은 기존 `보유 온기` 문구를 유지합니다.
- `migrateKioskUserCreditsToOrderLimits()`는 이용자목록 전체 백업 시트를 만든 뒤 활성 이용자의 한도를 10으로 맞추고 비활성 이용자 값은 보존합니다.
- API 계약은 `2026-08-25.2`, 서비스워커 캐시는 `kiosk-cache-v344`로 갱신하며 `scripts/test-kiosk-order-limit.js`로 한도·차감·환불 분기를 검사합니다.

### P104 구현 범위

- 이용신청 수정·건너뛰기·레거시 주간 순환·익명화는 변경된 실제 행 구간만 저장하며, 익명화된 신청과 비대기 신청의 대기 순번을 비웁니다.
- 개인정보 익명화는 `이용신청_임시백업_*`을 만든 뒤 저장 결과를 재조회합니다. 성공 또는 정상 자동 복구 후 임시 백업을 삭제하고, 삭제·복구 실패 때만 백업명과 수동 조치 상태를 반환합니다.
- 지난 주문 보관은 잠금 안에서 필수 헤더·키 누락·양쪽 중복·23/24열 호환성을 다시 확인하고, `주문내역_자동백업_*`과 기존 `주문보관_자동백업_*`을 영구 보관합니다.
- 보관 시트를 먼저 검증한 후 `주문내역` 전체를 지우지 않고 오늘 이전 행만 아래에서 위로 삭제하며, 오류가 잡히면 양쪽 원본을 자동 복구합니다.
- API 계약은 `2026-08-26.1`, 서비스워커 캐시는 `kiosk-cache-v345`이며 `scripts/test-data-safety.js`로 비연속 행·백업·검증·복구 실패 분기를 검사합니다.

### P105 구현 범위

- 관리자 취소와 이용자 직접 취소는 동일한 서버 트랜잭션을 사용합니다. 주문 전체의 이용자·토큰·총 온기·품목·수량·재고를 먼저 검증하고, 이용자 직접 취소는 모든 품목이 접수 상태 `N`일 때만 허용합니다.
- 주문내역·간식목록과 게스트 주문의 게스트크레딧에 작업용 임시 백업을 만든 뒤 재고 복구, 온기 1회 환불, 주문 전체 취소 상태를 배치 저장하고 재조회 검증합니다. 실패 시 모든 시트를 복원하며 정상 처리·정상 복구 후 임시 백업을 삭제합니다.
- 이미 전부 취소된 주문은 `alreadyCancelled: true`로 성공 응답하고 재고나 온기를 다시 변경하지 않습니다. 복구·정리 실패 때만 백업 시트명과 수동 조치 상태를 반환합니다.
- 신규 일반 키오스크와 배달왔삼 주문은 모두 UUID 기반 `orderToken`을 저장합니다. 이용자 직접 취소는 주문 종류와 관계없이 정확한 토큰이 필요하며, 기존 토큰 없는 일반 주문은 관리자만 취소할 수 있습니다.
- 제공 상태 변경도 동일 주문의 모든 품목을 함께 저장·검증하고 실패 시 원래 상태로 복구합니다. API 계약은 `2026-08-26.2`, 서비스워커 캐시는 `kiosk-cache-v346`이며 `scripts/test-order-cancellation-safety.js`로 검사합니다.

### P106 구현 범위

- `placeOrder`는 요청의 `perfDebug: 1`일 때만 개인정보 없는 `_timings`를 반환하며 잠금 대기, 주문·간식·이용자 조회, 검증, 주문·재고·온기·커밋·프로필·이메일 큐 구간을 분리합니다.
- 일반 키오스크는 기존 5분 이용자 읽기 캐시를 재사용합니다. 주문내역과 재고는 멱등성·수량 제한·재고 정합성을 위해 계속 실데이터를 읽습니다.
- 배달왔삼 알림은 새 주문 커밋 뒤 주문 잠금을 해제한 다음 별도 문서 잠금으로 적재합니다. 커밋 주문의 고유 주문번호를 이용해 주문 큐 전체 중복 읽기를 생략하고, 이용신청 알림의 중복 차단은 유지합니다.
- 이메일 큐는 스키마와 수신자를 10분 캐시하고 단일 `setValues`로 행을 추가합니다. 트리거는 선점 F:H와 결과 F:J만 갱신하며 기존 A:L 계약·1·5·15분 재시도·최대 4회 시도는 유지합니다.
- `scripts/stability-check.js`의 `performance` 모드는 staging 확인 후 일반·배달왔삼 각 10건, 멱등 재요청, 동시 5건을 실행·정리하고 warm p95 5초, cold 20초, 큐 p95 500ms 기준을 판정합니다. 운영 URL에서는 실행하지 않습니다.
- P106 배포 당시 API 계약은 `2026-08-27.1`, 서비스워커 캐시는 `kiosk-cache-v347`이었으며 `scripts/test-order-performance.js`와 `scripts/test-email-queue-performance.js`로 검사합니다. P108 이전 운영 계약·캐시는 P98·P94 보강 기준 `2026-08-27.2`, `kiosk-cache-v348`입니다.

### P108 구현 범위

- 주문 조회·이용자 취소·후기 등록/조회/수정·후기 사진 업로드는 공통 소유권 검사기로 주문번호·토큰·이용자와 다품목 행 일관성을 확인합니다. 상태 조회는 토큰이 필수이며 토큰 없는 과거 주문은 관리자만 처리합니다.
- 신규 주문은 유효한 `idempotencyKey`가 없으면 잠금과 시트 읽기 전에 거부합니다. 일반·배달왔삼 화면과 Mock API의 성공 불명확 재시도는 같은 키를 유지합니다.
- `ensureOrderHeaders`는 관리자 인증 뒤에만 실행하고 공개 후기 API의 헤더 자동 보정을 제거했습니다. `ALLOW_LEGACY_ADMIN_GET` 우회와 관련 진단을 삭제하고 `getOrdersToday`는 항상 공개 필드만 반환합니다.
- 간식 가격 1~15, 재고 0~30, 1인 제한 0~30과 양의 정수 ID를 서버·관리 화면·Mock에서 함께 검증합니다. 행사명은 1~20자, 굵게와 기존 6개 색상만 정규화해 저장합니다.
- 공개 예외는 `INTERNAL_ERROR`와 비식별 오류 ID로 응답하고 관리자 경로의 상세 진단은 유지합니다. 요청 로그에는 전체 파라미터·토큰·`guestKey`를 남기지 않습니다.
- API 계약은 `2026-08-27.3`, 서비스워커 캐시는 `kiosk-cache-v349`이며 `scripts/test-api-input-security.js`와 기존 P104~P106·후기 회귀검사를 통과했습니다. DB 열 마이그레이션은 없습니다.

### P109 구현 범위

- P105의 취소·제공 상태 자동복구가 끝내 실패하거나 임시 백업 삭제가 실패한 경우 Script Property에 비식별 경고를 남겨, 단일 API 응답이 사라진 뒤에도 관리자 운영점검에서 해당 주문과 백업 시트명을 찾을 수 있게 합니다.
- 기록에는 주문번호, 단계, 발생 시각, `recoveryRequired`, `cleanupRequired`, 백업 시트명과 수동 조치 필요 여부만 남기고 이름·전화번호·배송지·메모·토큰은 남기지 않습니다.
- 정상 취소에 별도 `환불 대기` 상태를 만들지 않습니다. 자동복구 실패처럼 실제 수동 확인이 필요한 경우만 경고 대상으로 삼으며 관리자 복원 버튼과 복원 API는 만들지 않습니다.
- 같은 주문·단계·백업 조합은 발생 횟수와 마지막 발생 시각만 갱신합니다. 관리자가 실제 수동 조치를 마친 뒤 경고를 확인 완료로 닫을 수 있지만 데이터 복원이나 백업 삭제를 API가 대신 수행하지는 않습니다.
- API 계약은 `2026-08-27.4`, 서비스워커 캐시는 `kiosk-cache-v350`이며 `scripts/test-recovery-alerts.js`와 취소 안전 회귀검사로 기록 지속성·중복 방지·개인정보 비기록·확인 완료를 검사합니다. DB 열·시트 마이그레이션은 없습니다.

### P110 예정 범위

- 브라우저가 보내는 `authProvider: kakao`와 `guestKey`만 신뢰하지 않고, 카카오 코드 교환 성공을 서버가 서명한 짧은 수명의 증명으로 주문·프로필·조회 요청에 전달합니다.
- 임의 `guestKey`로 카카오 보너스 온기를 받거나 다른 이용자의 주문을 조회하는 경로를 차단합니다. 기존 로그인 세션은 배포 전환 정책을 확정한 뒤 단계적으로 만료시킵니다.
- 공개 이용신청·주문 API에는 정상적인 중복 클릭·네트워크 재시도를 해치지 않는 범위의 간단한 요청 제한을 추가합니다. 카카오 변경은 영향이 크므로 P108·P109 뒤에 별도 배포합니다.

### P87 구현 범위

- `gas/60_Settings.gs`의 `buildGuestSettingsResponse`에서 불리언 설정값(`guestAllowRandomDisplayName`, `guestAllowMultipleOrders`) 파싱 시 `false || 'TRUE'` 평가로 인해 `true`로 왜곡되는 자바스크립트 기본값 처리 버그를 수정합니다.
- 시트에서 읽은 값이 boolean `false` 또는 문자열 `'FALSE'`인 경우 정확히 `false`로 클라이언트에 전달되도록 보정합니다.
- 주방 설정 저장 응답에 최신 설정값을 포함해 저장 직후 랜덤 주문표시명 세그먼트가 서버 기준 값으로 다시 렌더링되도록 보강합니다.
- `confirm.html`은 진입 시 `getGuestSettings`를 재조회해 오래된 `sessionStorage` 값 때문에 랜덤 이름 버튼이 남지 않도록 합니다.
- 주방 설정 저장 직후 `getGuestSettings()`를 재호출하지 않고 저장 요청 값으로 응답을 직접 구성해, 저장 성공 후 후속 재조회/캐시 작업 실패가 전체 저장 실패로 보이지 않게 합니다.
- 기본 배달지를 완전 공백으로 저장해도 `사무실 원탁` 기본값으로 되살리지 않고, 확인·완료 화면도 최신 게스트 설정을 다시 반영합니다.

### P88 구현 범위

- 새 주문 접수 시 담당자 Gmail 알림(`sendOrderNotification`)이 동기식으로 실행되어 발생하는 1~2초 주문 완료 지연을 완화하기 위해 운영설정에 알림 ON/OFF 스위치(`adminOrderEmailNotificationEnabled`)를 추가합니다.
- 알림 기능이 OFF일 때는 `MailApp.sendEmail` 호출을 생략하여 0.5초 이내 즉각적인 주문 완료 응답을 보장합니다.

### P89 구현 범위

- 게스트 `새 주문하기`는 크레딧·프로필 조회 완료를 기다리지 않고 즉시 메뉴로 이동하며, 메뉴에서 크레딧을 백그라운드 갱신합니다.
- 주문 이메일은 `이메일알림큐`에 적재하고 1분 트리거가 락 밖에서 발송하며, 실패 시 1·5·15분 간격으로 최대 3회 재시도합니다.
- 운영설정 조회는 읽기 전용으로 바꾸고 저장은 스크립트 락과 단일 `setValues`로 처리합니다.
- 주문 시트를 한 번 읽어 멱등성·진행 주문·수량 제한·순번을 계산하고, 재고를 배치 반영하며 `PENDING/COMMITTED/FAILED` 상태와 보상 복구를 적용합니다.
- 운영 점검에서 이메일 큐 시트·트리거·대기·실패·수신 주소를 진단하고 staging 안정성 검사에 주문 단계별 롤백 검증을 추가합니다.

### P90 구현 범위

- 공통 `.btn { display: flex; }`가 브라우저의 `hidden` 표시 규칙을 덮어쓰던 원인을 수정해 `[hidden] { display: none !important; }`를 공통 스타일에 명시합니다.
- 게스트 첫 화면의 랜덤 이름 버튼은 초기 HTML부터 숨기고, API에서 `guestAllowRandomDisplayName=true`를 명시적으로 받은 경우에만 표시합니다.
- 게스트·확인·완료 화면의 세션 설정도 명시적 `true`만 허용해 API 지연·실패 또는 누락 값에서 이전 허용 상태가 남지 않게 합니다.
- 서비스워커 캐시는 `kiosk-cache-v325`로 갱신합니다.

### P91 구현 범위

- 주문 내역의 작성 완료 후기에서 `후기 수정`을 열고 서버 원본의 스탬프·태그·한마디·공개 여부·사진을 불러옵니다.
- `getGuestReview`, `updateGuestReview`는 주문내역·주문보관의 주문번호와 토큰을 정확히 검증하고 작성 후 7일 동안 기존 후기 한 행만 수정합니다.
- 사진은 유지·교체·삭제를 구분하며 교체·삭제된 기존 Drive 파일은 후기 폴더 소유를 확인한 뒤 정리합니다.
- 직원 답글과 최초 작성 시각은 보존하고 `updatedAt`, `editCount`로 주문자·공개·관리자 화면에 수정 이력을 표시합니다.
- API 계약은 `2026-08-22.2`, 서비스워커 캐시는 `kiosk-cache-v329`로 갱신합니다.

### P93 구현 범위

- 이용신청 주차가 자동 순환되는 현재 방식 대신 관리자가 직접 대상 주차를 변경하거나 접수를 일시 중단하는 운영 방식을 검토합니다.
- 신청 내역을 스프레드시트처럼 한 화면의 목록으로 보고 여러 항목을 체크한 뒤 승인·대기·상태 변경 등을 처리하는 관리 UX를 검토합니다.
- 이용 일정·신청 상태·운영 안내 등을 배달왔삼 메인 화면에 공지하는 기능과 공개 범위·게시 기간을 논의합니다.
- 새 이용신청이 접수되면 담당자에게 이메일을 보내는 기능의 수신자, ON/OFF 설정, 중복 방지 및 비동기 발송 방식을 논의합니다.
- 내부 운영 중단 사유와 별도로 공개 안내문을 관리하는 기능을 검토합니다. 핵심 수동 운영·일시 중단 기능을 먼저 적용하고, 공개 공지는 후속 단계로 둡니다.
- 공개 안내는 표시 여부·제목·내용·게시 기간을 별도로 관리하며, 내부 행사 등 상세 사유나 개인정보가 외부에 노출되지 않도록 합니다.
- `이용신청` 22열은 유지하고, 주간 배정·완료 이력은 별도 `이용운영기록` 시트에 저장합니다.
- 신청 알림은 기존 `이메일알림큐`를 확장해 `notificationType`·`referenceId`로 중복을 방지하고 1분 비동기 트리거로 발송합니다.
- 공개 운영 중단 공지는 P96 후속 과제로 남기며 P93에서는 내부 운영 중단 설정만 구현합니다.
- 주간 운영 화면은 이름 중심 단일 목록으로 통합하고, 선택한 주차 운영 확정·확정 취소·서비스 완료 버튼의 역할과 크기를 분리합니다.
- 중복 운영 기록은 백업 후 추가 행을 `CANCELLED`로 보존하는 정리 API를 제공하며, 테스트 신청 표시·필터·확인 문구 기반 정리 UI를 추가합니다.

### P95 구현 범위

- `guestApplicationCapacity`는 주당 운영 안내 숫자로만 표시하고 신규 신청·승인·주간 운영 확정을 제한하지 않습니다. 신규 신청은 항상 `PENDING`, 기존 `WAITLIST`는 수동 보류 상태로 유지합니다.
- `rotateGuestApplicationWeekly()`는 기존 시간 트리거가 남아 있어도 신청 상태를 바꾸지 않고 종료합니다. 자동 순환·이번 주 중단 UI와 주간 설정 저장 버튼은 제거했습니다.
- 관리자 주간 운영은 운영 주차 시작일을 선택한 뒤 체크한 승인자를 확정하고, 확정 취소·서비스 제공 완료를 별도로 실행합니다. 승인 신청자 행의 `일정 추가`로 다른 주차를 추가할 수 있습니다.
- 신청자 목록은 이름·상태·운영 예정일·서비스 완료 횟수·상세 중심으로 단일화했습니다. `서비스 완료 N회`는 `이용운영기록`의 중복 제거된 `COMPLETED` 기록을 계산하며 시트 열은 추가하지 않습니다.
- 상태 필터는 `전체·검토 중·대기·승인·종료`로 줄이고 테스트 신청은 유지보수 영역의 `테스트 신청 보기`에서만 조회합니다. 오래된 정보 정리·테스트 신청 삭제·중복 기록 점검은 확인 모달을 거칩니다.

### P96 검토 범위

- 운영 주차 시작일과 신청자 운영 주차가 같을 때도 `선택한 주차 운영`처럼 상태를 추측하게 하는 문구 대신 `운영 예정: YYYY년 M월 D일`을 표시하고, 완료된 대상은 `서비스 완료: YYYY년 M월 D일 · 서비스 완료 N회`로 표시합니다.
- 중복 클릭 방지는 신청 관리 변경 작업별 공통 잠금으로 처리하되, 페이지 전체를 막지 않고 같은 작업 그룹의 버튼만 처리 중 비활성화합니다. 버튼에는 `aria-busy`를 반영하고 성공·실패·타임아웃 후 잠금을 복구합니다.
- 확정·확정 취소·서비스 완료·신청 상태 저장·운영 일정 추가·설정 저장·오래된 정보 정리·테스트 신청 표시·삭제·중복 기록 정리를 대상으로 합니다. 탭 이동·모달 열기·새로고침 같은 읽기·이동 버튼은 잠그지 않습니다.
- 프론트 요청 ID는 정상적인 새 작업과 응답 유실 재시도를 구분하도록 작업 내용이 같을 때 유지합니다. 서버에서는 기존 `LockService`, 상태 검사, `applicationId + serviceWeek` 중복 방어와 함께 신청 관리 변경 API의 `CacheService` 결과 캐시를 사용합니다.

### P97 검토 범위

- 신청자 행의 `상세` 버튼은 유지합니다. 목록에는 이름·상태·운영 날짜만 표시하고 연락처·배달 장소·신청 메시지는 상세 모달에서만 확인하는 개인정보 보호 관문입니다.
- P95에서 자동 순환을 폐기했으므로 상세 모달의 `이번 주 건너뛰기`는 제거 후보입니다. 운영 일정 변경은 행의 `일정 추가`와 운영 주차 선택으로 통일합니다.
- `테스트 신청으로 표시`는 일반 상태 변경 버튼과 분리해 고급 관리 영역 또는 상세 모달의 접기 영역으로 이동합니다. 일반 운영자가 실수로 테스트 표시를 누르지 않게 합니다.
- `테스트 신청 보기`와 `테스트 신청 삭제`는 둘 다 필요하지만 일반 화면에 항상 노출하지 않고 `테스트 신청 관리` 안에서 순서대로 표시합니다. 삭제는 기존 확인 모달·운영 기록 보존 조건을 유지합니다.
- 승인·반려·이용 중지·연락 완료·메모 저장은 실제 업무 기능이므로 유지하되, 현재 상태에 맞는 버튼만 보여 주어 모달의 시각적 복잡성을 줄입니다.

### P98·P94 배포 보강 범위

- 공개 신청서의 최소 입력, `이용 가능·확인할 신청·종료` 세 그룹, `신청자·설정·개인정보 파기` 탭, 행 안 `운영 추가`, 개인정보·메모 전용 상세 모달을 유지합니다.
- 운영 완료·확정 취소는 요청 ID를 중복 제거한 뒤 모든 운영 기록을 먼저 찾습니다. 누락 ID, 처리 불가능한 상태 또는 승인되지 않은 완료 대상이 하나라도 있으면 어떤 행도 저장하지 않습니다.
- 검증을 모두 통과한 요청만 한 번의 배치 쓰기로 저장합니다. 실패 요청은 관리자 로그와 요청 결과 캐시를 남기지 않으며, 성공한 같은 `requestId` 재시도는 기존 결과를 반환합니다.
- DB 시트·열은 변경하지 않습니다. 가짜 시트 자동검사는 `node scripts/test-guest-application-management.js`로 실행합니다.
- 배포 묶음은 `00_Config.gs`, `01_Router.gs`, `12_GuestApplications.gs`, `14_GuestApplicationOperations.gs`이며 API 계약은 `2026-08-27.2`, 서비스워커 캐시는 `kiosk-cache-v348`입니다. 운영 DB에서는 테스트 신청·강제 실패·대량 상태 변경을 수행하지 않습니다.

### P76 구현 범위

- `운영설정`에 `guestAllowRandomDisplayName` 키를 추가하고 기존 키가 없으면 `TRUE`로 보정합니다.
- 주방 설정에서 `주문표시명 자동 생성`을 허용·비허용으로 저장하며, 게스트·주문확인 화면은 이 값을 기준으로 랜덤 이름 버튼만 표시하거나 숨깁니다.
- 직접 입력한 주문표시명, 기존 로컬 저장값, 카카오 표시명, 주문 완료·조회·취소·후기 흐름은 유지합니다.

### P77 구현 범위

- 관리자 간식 신규 등록·수정 모달의 이미지 입력 라벨에 `ⓘ` 도움말을 추가합니다.
- 도움말은 `docs/handoff/kitchen-prep-visual-guidelines.md`의 기준을 요약해 안내하며, 반응형 모달·닫기·Esc·바깥 클릭을 지원합니다.
- 이미지 URL 입력·파일 업로드·간식 등록·수정 API에는 동작 변경이 없습니다.

### P72 구현 범위

- `getAdminDashboard`, `getKitchenDashboard`, `getAdminOrdersToday`은 관리자 토큰이 필요한 POST 조회로 제한하고 인쇄 화면에도 관리자 잠금 화면을 적용합니다.
- 공개 이용자·간식 API는 비활성·숨김 포함 요청을 무시하며, 공개 주문 피드는 호출판에 필요한 허용 필드만 반환합니다.
- 응답에 `apiContractVersion`과 `serverTime`을 넣고 관리자 진단에서 환경, 주간 순환 트리거, ScriptCache, 주요 구간 시간, 임시 호환 스위치를 확인합니다.
- `scripts/stability-check.js`는 운영 `read`와 staging 전용 `full`을 분리하고 동시 주문·재고 초과·취소 복원·후기·신청 멱등성을 검사합니다.
- 배포와 검증 절차는 `docs/system-stability.md`를 단일 실행 안내로 사용합니다.

### P71 구현 범위

- 키오스크 이용자·간식, 배달왔삼 운영 설정·최근 후기, 이용 신청 설정, 공개 후기, 전광판 및 주문 상태처럼 데이터 변경이 없는 조회에 30초 제한과 일시적 네트워크 오류 1회 재시도를 적용합니다.
- 키오스크 취소 주문 폴링은 5초 고정 간격 중첩 요청을 제거하고, 앞선 요청이 끝난 뒤 10초 후 다음 조회를 예약합니다.
- 공개 후기 화면은 공통 API 헬퍼를 사용하며 연결 실패를 후기 0개로 표시하지 않고 새로고침 가능한 오류 상태로 구분합니다.
- `getRecentReviews` 결과는 5분간 ScriptCache에 보관하고 후기 등록·답글·공개 상태 변경 시 공개 후기 캐시와 함께 무효화합니다.
- `reviews-public.html`에서도 공통 앱 스크립트를 불러 직접 진입 시 서비스 워커 최신 버전을 확인합니다.

### P60 구현 범위

- `guest-apply.html`, `reviews-public.html`, `guest.html`의 헤더 구조와 전체 높이, 탭 크기·위치는 동일하게 유지합니다.
- 일반 모드에서는 세 화면 모두 `배달왔삼` 제목과 `삼각지 카페 배달 서비스` 부제를 표시합니다.
- 로고는 데스크톱 42px·모바일 36px, 제목은 18px, 부제는 11~12px을 기준으로 하되 실제 화면에서 과밀하지 않게 조정합니다.
- 행사 모드에서는 주문 페이지에만 행사 엠블럼·행사명·`행사 간식 주문` 부제를 표시하고, 이용신청·고객 후기에는 추가 설정 API 호출 없이 기본 브랜드를 유지합니다.
- 긴 행사명은 한 줄 말줄임으로 제한하고 브랜드 영역의 최소 높이를 고정해 일반·행사 전환 시 헤더와 탭이 흔들리지 않게 합니다.
- 행사 엠블럼 적용 시 이미지 대체 텍스트도 행사명에 맞춰 변경합니다.
- sticky 상태에서는 콘텐츠와 구분되는 그림자만 보강하고, 높이가 변하는 축소 애니메이션은 적용하지 않습니다.
- `행사 운영 중` 상태 배지는 이번 범위에서 보류하며 실제 구분 필요성이 확인될 때 별도로 검토합니다.

### 보류 과제

| ID | 작업 | 상태 | 재검토 조건 |
|---|---|---|---|
| P15 | 배달왔삼 선택형 TTS 안내 | 보류 | 기본값 끔, 핵심 순간만 안내할 문구와 개인정보 범위가 확정될 때 |
| P85 | 주방 레시피 가이드 외부 연결 | 보류 | `recipe-guide`의 공개 배포 URL을 확정하고 독립 PWA·오프라인 이미지·TTS 흐름을 현장 기기에서 검증한 뒤, 주방 사이드바 바로가기와 간식별 `?recipe=` 직접 연결 범위를 결정할 때 |

- `placeOrder` 트랜잭션 구조 개편 is currently not registered as a task.

### 운영 모니터링

- GAS 429/503 또는 락 획득 대기 시간 초과가 재발하는지 관찰합니다.
- 특정 간식이 일시적으로 주문 불가로 표시되는 현상이 재발하면 판매 상태, 제공 대상, 15초 표시 캐시와 최종 원본 시트 검증을 함께 확인합니다.

## 문서 목차

| 문서 | 언제 읽는가 |
|---|---|
| [구조와 실행 흐름](docs/handoff/architecture.md) | 파일 역할, URL, GAS 배포 구조를 확인할 때 |
| [결정사항과 운영 규칙](docs/handoff/decisions.md) | 유지해야 할 UX·보안·데이터 정책을 확인할 때 |
| [데이터베이스 구조](docs/handoff/database-schema.md) | Google Sheets 시트와 열 계약을 변경하거나 점검할 때 |
| [검증 절차](docs/handoff/verification.md) | 현재 상태와 무관한 재사용 검증 절차를 확인할 때 |
| [완료 작업 로그](docs/handoff/work-log.md) | 완료 처리된 작업 결과를 확인할 때 |
| [활성 작업 문서 아카이브](docs/handoff/active-work-archive-2026-07-13.md) | 단일 기준 전환 전 활성 작업 상세가 필요할 때 |
| [혼합 작업 로그 아카이브](docs/handoff/work-log-archive-2026-07-13.md) | 단일 기준 전환 전 완료·대기 혼합 기록이 필요할 때 |
| [기존 핸드오프 스냅샷](docs/handoff/handoff-snapshot-2026-07-10.md) | 분리 전 상세 분석과 완료 기록이 필요할 때 |
| [이전 상세 기록](docs/handoff/handoff-archive.md) | 장기 작업 이력과 과거 판단 근거가 필요할 때 |
| [P21 대기자(WAITLIST) 구현 계획](docs/handoff/implementation-p21-waitlist.md) | 대기자 기능 구현 상세를 확인할 때 |
| [에셋 출처와 정리 현황](docs/handoff/asset-sources.md) | 이미지·폰트·PWA 아이콘의 출처와 상태를 확인할 때 |
| [관리자 운영 설명서](docs/admin-manual.md) | 관리자·주방·후기·이용 신청 화면을 실제로 운영할 때 |
| [프로포절 기능 검토서](docs/proposal-feature-review.md) | 공동모금회 사업계획에 맞춘 기능 범위와 단계별 구현 순서를 확인할 때 |
| [시스템 안정성 점검 안내](docs/system-stability.md) | staging 준비, API 보안 배포, 동시성 검사와 운영 관찰을 실행할 때 |
| [주방 준비 보드 시각자료 수칙](docs/handoff/kitchen-prep-visual-guidelines.md) | 주방 간식 이미지 제작·표시와 선택형 TTS를 설계하거나 수정할 때 |
| [레거시 GAS 원본](google-apps-script.md) | 분리 전 단일 GAS 파일과 비교가 필요할 때만 |

## AI 작업 원칙

1. **현재 상태와 다음 작업은 이 파일만 기준으로 판단합니다.**
2. 작업 영역에 따라 `architecture.md`, `decisions.md`, `database-schema.md`, `verification.md`만 추가로 읽습니다.
3. 실제 DB 구조를 추측하지 않습니다. 구조 변경 전에는 최신 DB 사본 또는 운영 점검 결과를 확인합니다.
4. 운영 중인 시트 열을 코드 가정에 맞춰 임의로 이동하거나 삭제하지 않습니다.
5. GAS를 수정하면 결합 구문 검사 후 새 버전을 배포하고, 정적 파일을 수정하면 서비스 워커 캐시 버전 상향 필요성을 확인합니다.
6. 모든 화면 변경은 [모바일 친화적 개발 원칙](docs/handoff/decisions.md#모바일-친화적-개발-원칙)을 확인하고 PC·390px·320px 회귀 범위를 함께 정합니다.
7. 완료 작업의 결과는 `work-log.md`에 한 번만 남기고, 긴 분석 원문은 `handoff-archive.md`로 보냅니다.
8. 문서 변경을 마치기 전에 `node scripts/check-handoff.js`를 실행합니다.

## 핸드오프 기록 안전 규칙

### 1. 작업 번호

- 작업 번호는 한 번 사용하면 완료·보류·폐기 후에도 다른 작업에 재사용하지 않습니다.
- 현재 사용이 확정된 마지막 번호는 `P110`이며, **새 작업은 `P111`부터 순서대로 부여합니다.**
- 기존 작업의 내용이 달라지면 같은 번호를 재활용하지 않고 새 번호를 발급한 뒤 이전 작업과의 관계를 적습니다.

### 2. 허용 상태

작업 상태는 다음 표현만 사용합니다.

`검토 대기` → `구현 대기` → `구현 중` → `구현 완료·배포 대기` → `배포 완료·수동 검증 대기` → `완료`

진행하지 않기로 한 작업은 `보류` 또는 `폐기`로만 기록합니다.

- 자동 테스트 통과를 `수동 검증 완료`로 기록하지 않습니다.
- 코드가 저장소에 있어도 GAS 실제 배포를 확인하지 못했다면 `배포 대기`를 유지합니다.
- 사용자가 배포 또는 수동검증 완료를 명확히 확인했을 때만 다음 상태로 바꿉니다.

### 3. 파일별 역할

- `handoff.md`: 현재 상태와 작업 상태를 기록하는 유일한 기준입니다. 완료·폐기 작업은 현재 표에 남기지 않습니다.
- `work-log.md`: `완료` 처리된 작업의 날짜와 결과만 기록합니다. 대기·보류 상태는 적지 않습니다.
- `verification.md`: 상태 표현 없이 반복해서 사용할 검증 절차만 기록합니다.
- `decisions.md`: 장기간 유지할 정책과 폐기한 대안의 이유를 기록합니다.
- `database-schema.md`: 실제 확인한 DB 구조와 아직 배포 전인 코드 계약을 명확히 구분합니다.
- `active-work.md`는 더 이상 사용하지 않습니다. 이전 내용은 날짜가 붙은 아카이브에서만 확인합니다.

### 4. 상태 변경 시 동시 갱신

- **새 작업·우선순위·배포·수동검증 상태 변경**: `handoff.md`의 현재 우선순위 표만 갱신
- **작업 완료**: 활성 목록에서 제거하고 `work-log.md`에 결과 추가
- **정책 변경**: `decisions.md`
- **DB 계약 변경**: 최신 헤더를 확인한 뒤 `database-schema.md`
- **에셋 추가·교체**: `asset-sources.md`

### 5. 기록 전 최종 점검

1. 같은 작업 번호가 다른 의미로 사용되지 않았는지 확인합니다.
2. 현재 우선순위 표에 `완료`·`폐기` 항목이 남아 있지 않은지 확인합니다.
3. `verification.md`에 `완료`·`대기` 상태를 기록하지 않았는지 확인합니다.
4. 실제 배포·수동검증을 추측해 완료 처리하지 않았는지 확인합니다.
5. 서비스 워커 캐시 버전과 DB 열 수처럼 코드로 확인 가능한 값은 실제 파일과 대조합니다.
6. `node scripts/check-handoff.js`가 성공하는지 확인합니다.
