# 배달왔삼 키오스크 프로젝트 핸드오프

> 마지막 갱신: 2026-08-03

## 현재 상태

- **치명 이슈**: 현재 운영 중 재현된 치명 버그는 없습니다. 다만 2026-07-25 정적 코드 점검에서 온기보드 개인정보 응답, 마일스톤 계산, 1인당 수량 제한의 보강 작업을 차기 우선순위로 등록했습니다.
- **운영 DB 기준**: `주간보호 매점DB (2).xlsx`의 실제 시트 10개와 1행 헤더 대조 및 신규 `이용신청` 22열(A:V) 시트의 생성 및 운영 검증을 완료했습니다.
- **GAS 구조**: `gas/*.gs` 기능별 분리를 완료했으며 같은 Apps Script 프로젝트 안에서 함께 배포합니다.
- **정적 캐시**: `service-worker.js`의 현재 캐시 버전은 `kiosk-cache-v295`입니다.
- **최근 구현**: 배달왔삼 이용 신청 공개 페이지, 관리자 신청 탭, `이용신청` 22열 계약, 멱등 접수, 중복 연락처 차단, 30일 만료 익명화, 기본 5명·1~100명 정원 조절, 대기자 기능 및 발달장애인 AAC 답글 UX, 내 주문 내역 후기 상태 보존(P22)을 완료했고, 직원 후기 관리 화면의 AAC 프리셋 복구 및 TTS 이모티콘 필터링 정제(P27)를 완료했으며, 답글 작성 2x2 이모티콘 그리드 개편 및 화면별 이미지 렌더링 연동(P28)을 완료하고, 관리자 페이지(admin.html) CSS/JS 외부 파일 분리 리팩토링(P29), 주방 페이지(kitchen.html) CSS/JS 외부 파일 분리 리팩토링(P30), 게스트 주문 화면(guest.html) CSS/JS 외부 파일 분리 리팩토링(P32), 직원 후기 관리 화면(reviews.html) CSS/JS 외부 파일 분리 리팩토링(P33), 지난 주문 보관 명시적 확인 오류 조치(P31), 내 주문 확인 화면 지난 보관 주문 더보기(Lazy Loading - P40), 장애인식 개선 캠페인/행사용 간식 모드 스위칭(P24), 관리자 게스트/행사 간식 및 순서 3단 분리 개편(P41), 게스트 행사명 텍스트 부분 서식(색상/굵기) 툴바 및 20자 1줄 보호(P42), 행사 모드 커스텀 엠블럼 설정(Base64)(P43), 배달왔삼 특정 간식 1인당 수량 제한 기능(P44), 가상 거래도구 '온기(❤️)' 전환, 달곰이 대원 브랜딩 UX 강화(P45), GAS 백엔드 및 배달왔삼/키오스크 속도 최적화(P46), 공개 3개 탭 네비게이션 통합 및 배달왔삼 키오스크 동선 보호(P48), 관리자/주방 L자형 이중 네비게이션, 모바일 슬라이딩 드로어 메뉴 및 주방 요약 카드 50:50 균등 분할(P55), 데스크톱 UX/UI 2차 최적화(P56), 모바일 헤더 UX/UI 개편 및 주방 운영 탭 클릭 방해 원인 조치(P57), 관리자·주방 API 배치 조회와 ScriptCache 2차 최적화(P58), 공개 서비스 공통 탭 헤더 통합(P59), 공개 서비스 헤더 브랜드 영역 확장(P60), 배달왔삼 이름 이야기 모달(P61), **이용신청 화면 정보 구조와 폼 가독성 개선(P62)** 구현을 완료했습니다.
- **외부 상태 주의**: 저장소에서는 GAS 실제 배포 여부와 운영 시트 생성 여부를 확인할 수 없습니다. 사용자 확인 전에는 항상 `배포·운영 검증 대기`로 기록합니다.
- **프로포절 기능 검토**: 사회복지공동모금회 지원사업 계획에 필요한 운영회차·역할수행·성과평가·다회용 컵·협력기관 관리 범위를 `docs/proposal-feature-review.md`에 정리했습니다. 구현은 기존 주문 흐름과 분리된 사업관리 계층으로 진행합니다.

## 현재 우선순위

이 표가 **현재 작업 상태의 유일한 기준**입니다. 상태를 다른 문서에 반복해서 기록하지 않습니다.

| 순서 | ID | 작업 | 상태 | 다음 행동 | 검증 절차 |
|---:|---|---|---|---|---|
| 1 | P34 | 주문 확인 화면(confirm.html) CSS/JS 외부 파일 분리 리팩토링 | 구현 완료·배포 대기 | GitHub Pages 반영 후 주문 확인 화면 최종 확인 | `confirm.html` 로드, 수량 변경·삭제 및 주문 완료 흐름 정상 동작 확인 |
| 2 | P35 | 주문 완료 화면(complete.html) CSS/JS 외부 파일 분리 리팩토링 | 구현 완료·배포 대기 | GitHub Pages 반영 후 주문 완료 화면 최종 확인 | `complete.html` 외부 파일 로드, 완료 정보 표시·취소 확인창·자동 복귀 정상 동작 확인 |
| 3 | P36 | 메뉴 선택 화면(menu.html) JS 외부 파일 분리 리팩토링 | 구현 완료·배포 대기 | GitHub Pages 반영 후 메뉴 선택 화면 최종 확인 | `menu.html` 외부 파일 로드, 간식 선택·합계 갱신·주문 확인 이동 정상 동작 확인 |
| 4 | P37 | 내 주문 내역 화면(guest-orders.html) CSS/JS 외부 파일 분리 리팩토링 | 구현 완료·배포 대기 | GitHub Pages 반영 후 내 주문 확인 화면 최종 확인 | 외부 파일 로드, 주문 내역·상태 표시, 후기 모달 및 주문 상세 이동 정상 동작 확인 |
| 5 | P38 | 이용 신청 화면(guest-apply.html) CSS/JS 외부 파일 분리 리팩토링 | 구현 완료·배포 대기 | GitHub Pages 반영 후 이용 신청 화면 최종 확인 | 외부 파일 로드, 필수 입력·동의 및 로컬 목업 신청 성공 화면 정상 동작 확인 |
| 6 | P39 | 현황판 화면(board.html) CSS/JS 외부 파일 분리 리팩토링 | 구현 완료·배포 대기 | GitHub Pages 반영 후 실시간 간식 호출 전광판 최종 확인 | 외부 파일 로드, 주문 상태 분류·취소 제외 및 10초 자동 갱신 정상 동작 확인 |
| 7 | P59 | 공개 서비스 탭 헤더 통합(이용신청·고객 후기·주문하기) | 구현 완료·배포 대기 | GitHub Pages 반영 후 세 공개 화면 헤더 최종 확인 | 데스크톱·모바일 크기 일치, sticky 동작, 활성 탭 및 화면 이동 정상 확인 |
| 8 | P60 | 공개 서비스 헤더 브랜드 영역 확장 | 구현 완료·배포 대기 | GitHub Pages 반영 후 일반·행사 모드 헤더 최종 확인 | 일반·행사 모드에서 데스크톱·390px·320px 헤더 크기, 말줄임, 탭 위치 및 접근성 확인 |
| 9 | P61 | 배달왔삼 이름 이야기 모달 | 구현 완료·배포 대기 | GitHub Pages 반영 후 세 공개 화면의 브랜드 모달 최종 확인 | 로고 버튼, 데스크톱 중앙·모바일 하단 배치, 닫기·바깥 클릭·Esc·포커스 복귀 및 가로 넘침 확인 |
| 10 | P62 | 이용신청 화면 정보 구조와 폼 가독성 개선 | 구현 완료·배포 대기 | GitHub Pages 반영 후 이용신청 화면 최종 확인 | 히어로 균형, 요약 정보 재사용, 폼 구획, 개인정보 안내와 신청·대기·마감 상태 확인 |
| 11 | P63 | 공개 후기 민감 닉네임 중립 호칭 처리 | 구현 완료·배포 대기 | GAS `@120` 반영 완료, GitHub Pages 반영 후 공개 후기 화면 최종 확인 | 원본·관리자 이름 유지, 공개 API·화면에서 민감 표현이 고정 중립 호칭으로 표시되는지 확인 |
| 12 | P64 | 공개 후기 탐색성과 모바일 가독성 개선 | 구현 완료·배포 대기 | GitHub Pages 반영 후 후기 필터·더보기·모바일 화면 최종 확인 | 전체·사진·답글 개수와 12개씩 더보기, 카드 정보 순서, 태그 축약, 달곰이 단일 엠블럼, 320px 눈금 및 하단 버튼 확인 |
| 13 | P65 | 후기 사진 닉네임 공개 확인 UX | 구현 완료·배포 대기 | GitHub Pages 반영 후 실제 휴대폰 사진 1장으로 사용자 수동 확인 | 안내문·확대 미리보기·공개 확인 체크, 미확인 제출 차단, 비공개 전환·사진 삭제 시 초기화 확인 |
| 14 | P66 | 프로포절 실행용 사업관리·성과관리 기능 | 검토 대기 | `docs/proposal-feature-review.md`의 1단계 범위와 평가 기준을 확정한 뒤 데이터 계약 설계 | 운영회차·역할배정·4단계 관찰·익명 참여 ID·기본 KPI가 기존 주문 흐름을 방해하지 않는지 검토 |

### P60 구현 범위

- `guest-apply.html`, `reviews-public.html`, `guest.html`의 헤더 구조와 전체 높이, 탭 크기·위치는 동일하게 유지합니다.
- 일반 모드에서는 세 화면 모두 `배달왔삼` 제목과 `삼각지 카페 배달 서비스` 부제를 표시합니다.
- 로고는 데스크톱 42px·모바일 36px, 제목은 18px, 부제는 11~12px을 기준으로 하되 실제 화면에서 과밀하지 않게 조정합니다.
- 행사 모드에서는 주문 페이지에만 행사 엠블럼·행사명·`행사 간식 주문` 부제를 표시하고, 이용신청·고객 후기에는 추가 설정 API 호출 없이 기본 브랜드를 유지합니다.
- 긴 행사명은 한 줄 말줄임으로 제한하고 브랜드 영역의 최소 높이를 고정해 일반·행사 전환 시 헤더와 탭이 흔들리지 않게 합니다.
- 행사 엠블럼 적용 시 이미지 대체 텍스트도 행사명에 맞춰 변경합니다.
- sticky 상태에서는 콘텐츠와 구분되는 그림자만 보강하고, 높이가 변하는 축소 애니메이션은 적용하지 않습니다.
- `행사 운영 중` 상태 배지는 이번 범위에서 보류하며 실제 구분 필요성이 확인될 때 별도로 검토합니다.

### 보류 과제

| ID | 작업 | 상태 | 재검토 조건 |
|---|---|---|---|
| P15 | 배달왔삼 선택형 TTS 안내 | 보류 | 기본값 끔, 핵심 순간만 안내할 문구와 개인정보 범위가 확정될 때 |

- `placeOrder` 트랜잭션 구조 개편 is currently not registered as a task.

### 운영 모니터링

- GAS 429/503 또는 락 획득 대기 시간 초과가 재발하는지 관찰합니다.
- 특정 간식이 일시적으로 주문 불가로 표시되는 현상이 재발하면 판매 상태, 제공 대상, 15초 표시 캐시와 최종 원본 시트 검증을 함께 확인합니다.

## 문서 목차

| 문서 | 언제 읽는가 |
|---|---|
| [구조와 실행 흐름](docs/handoff/architecture.md) | 파일 역할, URL, GAS 배포 구조를 확인할 때 |
| [결정사항과 운영 규칙](docs/handoff/decisions.md) | 유지해야 할 UX·보안·데이터 정책을 확인할 때 |
| [데이터베이스 구조](docs/handoff/database-schema.md) | Google Sheets 시트와 열 계약을 변경하거나 점검할 때 |
| [검증 절차](docs/handoff/verification.md) | 현재 상태와 무관한 재사용 검증 절차를 확인할 때 |
| [완료 작업 로그](docs/handoff/work-log.md) | 완료 처리된 작업 결과를 확인할 때 |
| [활성 작업 문서 아카이브](docs/handoff/active-work-archive-2026-07-13.md) | 단일 기준 전환 전 활성 작업 상세가 필요할 때 |
| [혼합 작업 로그 아카이브](docs/handoff/work-log-archive-2026-07-13.md) | 단일 기준 전환 전 완료·대기 혼합 기록이 필요할 때 |
| [기존 핸드오프 스냅샷](docs/handoff/handoff-snapshot-2026-07-10.md) | 분리 전 상세 분석과 완료 기록이 필요할 때 |
| [이전 상세 기록](docs/handoff/handoff-archive.md) | 장기 작업 이력과 과거 판단 근거가 필요할 때 |
| [P21 대기자(WAITLIST) 구현 계획](docs/handoff/implementation-p21-waitlist.md) | 대기자 기능 구현 상세를 확인할 때 |
| [에셋 출처와 정리 현황](docs/handoff/asset-sources.md) | 이미지·폰트·PWA 아이콘의 출처와 상태를 확인할 때 |
| [관리자 운영 설명서](docs/admin-manual.md) | 관리자·주방·후기·이용 신청 화면을 실제로 운영할 때 |
| [프로포절 기능 검토서](docs/proposal-feature-review.md) | 공동모금회 사업계획에 맞춘 기능 범위와 단계별 구현 순서를 확인할 때 |
| [레거시 GAS 원본](google-apps-script.md) | 분리 전 단일 GAS 파일과 비교가 필요할 때만 |

## AI 작업 원칙

1. **현재 상태와 다음 작업은 이 파일만 기준으로 판단합니다.**
2. 작업 영역에 따라 `architecture.md`, `decisions.md`, `database-schema.md`, `verification.md`만 추가로 읽습니다.
3. 실제 DB 구조를 추측하지 않습니다. 구조 변경 전에는 최신 DB 사본 또는 운영 점검 결과를 확인합니다.
4. 운영 중인 시트 열을 코드 가정에 맞춰 임의로 이동하거나 삭제하지 않습니다.
5. GAS를 수정하면 결합 구문 검사 후 새 버전을 배포하고, 정적 파일을 수정하면 서비스 워커 캐시 버전 상향 필요성을 확인합니다.
6. 모든 화면 변경은 [모바일 친화적 개발 원칙](docs/handoff/decisions.md#모바일-친화적-개발-원칙)을 확인하고 PC·390px·320px 회귀 범위를 함께 정합니다.
7. 완료 작업의 결과는 `work-log.md`에 한 번만 남기고, 긴 분석 원문은 `handoff-archive.md`로 보냅니다.
8. 문서 변경을 마치기 전에 `node scripts/check-handoff.js`를 실행합니다.

## 핸드오프 기록 안전 규칙

### 1. 작업 번호

- 작업 번호는 한 번 사용하면 완료·보류·폐기 후에도 다른 작업에 재사용하지 않습니다.
- 현재 사용이 확정된 마지막 번호는 `P66`이며, **새 작업은 `P67`부터 순서대로 부여합니다.**
- 기존 작업의 내용이 달라지면 같은 번호를 재활용하지 않고 새 번호를 발급한 뒤 이전 작업과의 관계를 적습니다.

### 2. 허용 상태

작업 상태는 다음 표현만 사용합니다.

`검토 대기` → `구현 대기` → `구현 중` → `구현 완료·배포 대기` → `배포 완료·수동 검증 대기` → `완료`

진행하지 않기로 한 작업은 `보류` 또는 `폐기`로만 기록합니다.

- 자동 테스트 통과를 `수동 검증 완료`로 기록하지 않습니다.
- 코드가 저장소에 있어도 GAS 실제 배포를 확인하지 못했다면 `배포 대기`를 유지합니다.
- 사용자가 배포 또는 수동검증 완료를 명확히 확인했을 때만 다음 상태로 바꿉니다.

### 3. 파일별 역할

- `handoff.md`: 현재 상태와 작업 상태를 기록하는 유일한 기준입니다. 완료·폐기 작업은 현재 표에 남기지 않습니다.
- `work-log.md`: `완료` 처리된 작업의 날짜와 결과만 기록합니다. 대기·보류 상태는 적지 않습니다.
- `verification.md`: 상태 표현 없이 반복해서 사용할 검증 절차만 기록합니다.
- `decisions.md`: 장기간 유지할 정책과 폐기한 대안의 이유를 기록합니다.
- `database-schema.md`: 실제 확인한 DB 구조와 아직 배포 전인 코드 계약을 명확히 구분합니다.
- `active-work.md`는 더 이상 사용하지 않습니다. 이전 내용은 날짜가 붙은 아카이브에서만 확인합니다.

### 4. 상태 변경 시 동시 갱신

- **새 작업·우선순위·배포·수동검증 상태 변경**: `handoff.md`의 현재 우선순위 표만 갱신
- **작업 완료**: 활성 목록에서 제거하고 `work-log.md`에 결과 추가
- **정책 변경**: `decisions.md`
- **DB 계약 변경**: 최신 헤더를 확인한 뒤 `database-schema.md`
- **에셋 추가·교체**: `asset-sources.md`

### 5. 기록 전 최종 점검

1. 같은 작업 번호가 다른 의미로 사용되지 않았는지 확인합니다.
2. 현재 우선순위 표에 `완료`·`폐기` 항목이 남아 있지 않은지 확인합니다.
3. `verification.md`에 `완료`·`대기` 상태를 기록하지 않았는지 확인합니다.
4. 실제 배포·수동검증을 추측해 완료 처리하지 않았는지 확인합니다.
5. 서비스 워커 캐시 버전과 DB 열 수처럼 코드로 확인 가능한 값은 실제 파일과 대조합니다.
6. `node scripts/check-handoff.js`가 성공하는지 확인합니다.
